# Test Progress

Last updated: 2026-07-17

## Summary

- Testing Mode: active.
- Discovery: in progress.
- Modules inventoried: 20 source-backed modules; completeness not yet asserted.
- Modules complete: 0.
- Latest strict cross-module verification: 22 safe source/mock tests executed; 19 Passed and only the three recorded blockers failed (`BUG-002`, `BUG-003`, `BUG-038`). Ten Playwright QA files passed syntax only and were not executed because the required in-app Browser is unavailable and some scripts mutate LAN data.
- UI contract: 204 native buttons audited, 198 reachable from active `App.tsx`; no missing/no-op action or invalid form-submit wiring found. Live click/render/role/viewport execution remains Blocked.
- Latest local UI batch: global Capy loading window randomly selects one of reference images 1/2/4 per loading session; 256px optimized assets are high-priority preloaded and decoded before paint, while attendance reminders use reference image 3. `CAPYBARA_LOADING_UI_OK`, app TypeScript, UI-button/idle-disabled regressions and the 697-module production build all Passed. In-app Browser discovery still returns `[]`, so live visual/viewport execution remains pending.
- Latest production build/deploy: Passed with 697 modules; app TypeScript and the mandatory Supabase bundle guard Passed. Live assets are `index-DyOxfcGs.js`/`index-CvUuwReK.css` on deployment `dpl_6SBUcHGg1VckVodC8oioWZAQoDiu`. Production HTML contains the three high-priority loading preloads and all four Capy PNGs return HTTP 200. Existing Browserslist/Tailwind/ExcelJS/large-chunk warnings remain.
- Bug tracker: 58 evidence-backed entries; BUG-002/003/038 remain `Blocked — Requires Business Decision`; BUG-054 through BUG-058 are deployed pending their listed visual/device/integration checks.
- Coverage: not measured; no percentage claimed.

## Timeline

| Time (Asia/Bangkok) | Batch | Result/evidence |
|---|---|---|
| 2026-07-17 | MOD20-TC-CAPY-01/02 | Added four validated/optimized 256×256 RGBA mascot assets, high-priority HTML preload plus pre-paint decode/selection, a root interaction/request-aware loading overlay with random image 1/2/4 selection, and image 3 on both check-in/check-out reminder surfaces. `CAPYBARA_LOADING_UI_OK`, no-emit TypeScript, 204-button contract, idle-disabled regression, 697-module production/prebuilt builds and bundle guards Passed. Deployment `dpl_6SBUcHGg1VckVodC8oioWZAQoDiu` is Ready/aliased; live HTML/bundle/four assets return expected evidence. Browser availability is `[]`, so no signed-in visual pass is claimed. No business rule, schema or data changed. |
| 2026-07-11 | Entry checks | `git status --short` showed a heavily dirty worktree; preserved as user-owned. |
| 2026-07-11 | Workflow lookup | `docs/testing/` absent from worktree, HEAD, Git history, and all branches. Baseline tracking workflow created from explicit requirements. |
| 2026-07-11 | Initial discovery | Confirmed React/Vite/Supabase architecture, role helpers, page/module surfaces, migrations, and existing Node/SQL QA assets. |
| 2026-07-11 | MOD-02 discovery | Confirmed 22 canonical hash routes, two legacy aliases, five roles, default-page rules, and current `canAccessPage` branches from source. Existing matrix has 90 checks but omits three admin-only routes. |
| 2026-07-11 | Browser setup | Blocked: required in-app Browser is unavailable; available-browser list was empty. UI cases remain unexecuted and are not marked Passed. |
| 2026-07-11 | MOD02-TC-BUILD-01 | Passed: `npx.cmd tsc -p tsconfig.app.json --noEmit --incremental false`; exit 0 in 14.6 s with no diagnostics. |
| 2026-07-11 | MOD02-TC-SRC-01 attempt 1 | Test harness failed to parse at line 20 (`missing )`); no application assertion ran and no product bug was inferred. |
| 2026-07-11 | MOD02-TC-SRC-01 attempt 2 | Harness extracted strings outside the route array, producing an invalid mismatch; no product bug inferred. |
| 2026-07-11 | MOD02-TC-SRC-01 attempt 3 | Valid failure confirmed BUG-001: permission matrix covers 19/22 canonical routes and omits three admin-only routes. |
| 2026-07-11 | BUG-001 fix | Added only the three omitted routes to `allPages`; no role/permission/business logic changed; verification pending. |
| 2026-07-11 | BUG-001 reproduction verification | Passed: static authorization check exit 0 and confirmed all 22 routes synchronized across type/hash/render/matrix. UI behavior remains unexecuted due Browser blocker. |
| 2026-07-11 | BUG-001 targeted regression | Passed: no-emit TypeScript check exit 0 in 11.1 s with no diagnostics. BUG-001 closed; UI execution remains separately Blocked. |
| 2026-07-11 | MOD02-TC-DB-01 discovery | Needs Business Confirmation: latest DB migrations grant managers all-branch scope, while login/app/dashboard code uses profile/assignment-scoped branch IDs. No permission logic changed; other cases continue. |
| 2026-07-11 | MOD02-TC-DB-02 | Failed: inactive-profile guard absent from shared DB authorization helpers despite explicit old-session lock contract. BUG-002 High recorded as `Blocked — Requires Business Decision`; no schema/permission change made. |
| 2026-07-11 | MOD02-TC-LAN-01 | Failed: invalid/missing LAN token can fall back to spoofable identity/role headers; privileged routes trust the role. BUG-003 Critical recorded as `Blocked — Requires Business Decision`; API auth contract unchanged. |
| 2026-07-11 | Final targeted regression | Passed combined batch: syntax checks for four QA/test scripts, 22-route static synchronization, and TypeScript no-emit; exit 0 in 10.9 s. |

## Active batch

### 2026-07-16 configured SKU deletion diagnosis

- Owner reports that a SKU deleted in Admin remains visible in inventory.
- Source confirms Admin removes the item locally and writes a durable cloud tombstone (`active=false`, `deleted_at`).
- Pre-fix `node scripts/test-product-delete-tombstone.mjs` exits 1: `mergeCloudIntoBase()` discards the tombstone before caching, `getProducts()` rebuilds every built-in SKU as active, and `calculateStock()` does not filter deleted/inactive products.
- Read-only anonymous REST returned zero rows because the products RLS policy is authenticated-only; no production row was changed and no absence/presence claim is inferred from that response.
- Owner then explicitly authorized the fix: an Admin-deleted SKU must also disappear from inventory.
- The product cache now preserves tombstones separately from visible products, filters their IDs from the built-in catalog/current stock, inbound, processing, packing and POS inputs, and keeps deleted product metadata available only for old document/history lookup.
- Post-fix `PRODUCT_DELETE_TOMBSTONE_OK`, inventory/report, manager inventory, pagination, POS/report consistency, shift reconciliation, business guards, 204-button contract, deploy-polish and app TypeScript all pass.
- Full production build passes with 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-DOyCiHFj.js)`; CSS remains `index-CMcoy5E8.css`. Local web and API health return 200.
- No database mutation, migration or deployment was performed. The in-app Browser control surface is unavailable in this session, so a real Admin delete plus second-device realtime visual check remains pending.

### 2026-07-15 deploy polish and shift-leader business correction

- Browser skill initialization was retried exactly as required. Runtime setup succeeded, `agent.browsers.get("iab")` failed, bootstrap troubleshooting was read, and the single discovery call returned `[]`. Live mobile/n8n clicking remains Blocked; no alternate browser backend was used.
- Pre-fix `node scripts/test-deploy-ui-business-polish.mjs` failed 10 source-backed assertions: report technical notes, verbose export copy, four inventory count cards, manager branch-invoice drilldown, leader ranking using personal rather than session revenue, unbounded shift-session reads, fragile order-image export, archive mobile locks, and long-name wrapping.
- BUG-041 through BUG-044 were recorded from owner screenshots plus source evidence. Removing manager invoice drilldown and inventory cards are direct owner-authorized presentation/permission changes, not inferred business redesigns. The active fix must not alter sales, stock, payroll, or attendance formulas.
- KPI reverse audit: `node scripts/test-kpi-reward-reverse-audit.mjs` failed three reproducible assertions. BUG-045 confirms that direct POS sales currently apply daily/weekly reward gates per receipt instead of once on the employee's total day. Existing monetary thresholds are source-backed and remain unchanged; only the aggregation defect is in scope.
- Targeted fixes verified: `DEPLOY_UI_BUSINESS_POLISH_OK` (including a functional two-session revenue fixture), `KPI_REWARD_REVERSE_AUDIT_OK`, `REPORT_TOOLBAR_MOBILE_OK`, `BUSINESS_COMPETITION_KPI_OK`, `OPERATIONS_UX_PERFORMANCE_OK`, `CRITICAL_LOAD_FAILURE_VISIBILITY_OK`, `REPORT_INVENTORY_UX_OK`, `UI_BUTTON_CONTRACT_OK` (195 total/189 reachable), and app TypeScript all pass. BUG-041/042/043 remain pending real visual/live-data verification; BUG-044/045 are source-verified closed.
- Final focused regression: 14/14 commands passed, including report finalization, shift/Zalo, n8n queue, pagination, core business guards and 22-route authorization synchronization. LAN server syntax passed. `npm.cmd run build` passed in 31.3s: 696 modules, deploy bundle emitted. Existing non-blocking warnings remain for stale Browserslist data, Tailwind's report-only input finding no utility classes, ExcelJS `eval`, and chunks over 500 kB.

MOD-06/MOD-08/MOD-10/MOD-13/MOD-17 are now the user-prioritized implementation/testing batch. The user explicitly requested the attendance-window and shift-opening workflow changes on 2026-07-13 and explicitly prohibited deployment until local testing is complete. Existing MOD-02 blockers remain unchanged and are not being modified in this batch.

| Time (Asia/Bangkok) | Batch | Result/evidence |
|---|---|---|
| 2026-07-13 | User-reported operational issues intake | Scope recorded in `code.md`: iPhone attendance/photo/export, unlimited attendance time, accurate GPS/time, automatic shift opening, report-by-shift, inventory readability/units, ranking/mobile overflow, realtime refresh, Sunday reminder, and Zalo handoff. No issue is yet marked verified/fixed in this batch. |
| 2026-07-13 | Deployment decision | User explicitly said not to deploy; all work is local-only until they test and approve. |
| 2026-07-13 | MOD06/MOD17 reproduction | `node scripts/test-mobile-attendance-export.mjs` failed with four source-backed assertions: no iPhone-safe attendance image decoder, device-clock-only attendance time, no native file sharing helper, and report data-URL download followed by unconditional success. BUG-004 High and BUG-005 Medium recorded. |
| 2026-07-13 | BUG-004/005 targeted fix verification | Passed: `node scripts/test-mobile-attendance-export.mjs` → `MOBILE_ATTENDANCE_EXPORT_OK`; `npx.cmd tsc -p tsconfig.app.json --noEmit --incremental false`; `node --check scripts/lan-server.mjs`; combined exit 0 in 18.5s. Direct iPhone 11 verification remains pending, so neither bug is closed yet. |
| 2026-07-13 | MOD10/MOD13 reproduction | `node scripts/test-report-inventory-ux.mjs` failed on seven pre-fix assertions. BUG-006 records the mobile ranking overflow/content issue and BUG-007 records negative-zero display. Report scope and kg/g input are explicit enhancements, not misclassified as bugs. |
| 2026-07-13 | MOD10/MOD13 targeted verification | Passed: `node scripts/test-report-inventory-ux.mjs` → `REPORT_INVENTORY_UX_OK`; TypeScript no-emit exit 0. Added Total day/Ca 1/Ca 2 report scopes and shift-specific export, sales-only ranking without hours, one-column mobile dense list, no stock meter, negative-zero normalization, kg/g entry conversion, and decimal warning. Real UI/mobile QA pending. |
| 2026-07-13 | MOD08/realtime/reminders verification | Passed: `node scripts/test-shift-realtime-reminders.mjs` → `SHIFT_REALTIME_REMINDERS_OK`; TypeScript no-emit exit 0. Leader check-in auto-opens the operational shift, Handover self-recovers without a manual Receive button, sequence resets by business date, Today step 1/final step are opening/closing photos, Today sales/shift data has realtime plus 8-second fallback, and Sunday-only animated capybara reminder is present. Multi-device integration/UI verification pending. |
| 2026-07-13 | Full local production build | First run reached Vite transform but exceeded the 60s command timeout; this was a harness timeout, not a product failure. Rerun with 120s passed: 693 modules transformed, production bundle created in 70.3s. Warnings remain for stale Browserslist data, ExcelJS `eval`, and large chunks; no deploy was performed. |
| 2026-07-13 | Browser UI setup retry | Browser skill bootstrap succeeded, but `agent.browsers.get("iab")` reported unavailable and the single allowed discovery call returned `[]`. Real UI/mobile cases remain Blocked; no alternate browser backend was substituted. |
| 2026-07-13 | Zalo GMF contract review | Official docs confirm group text messaging, member lookup, OA OAuth/refresh flow and OA image upload, but no public GMF image-message contract was found. Automatic image sending is `Blocked — Requires Zalo API/Permission Confirmation`; setup and secret handling are documented in `docs/ZALO_GMF_SETUP.md`. |
| 2026-07-13 | Final local regression | Passed 6/6: `MOBILE_ATTENDANCE_EXPORT_OK`, `REPORT_INVENTORY_UX_OK`, `SHIFT_REALTIME_REMINDERS_OK`, `AUTH_STATIC_OK (22 routes)`, TypeScript no-emit, and LAN server syntax. No production data was touched and no deployment was performed. |
| 2026-07-13 | Phone LAN access diagnosis | User reported the phone could not enter the local build. Source confirms Vite/API bind to `0.0.0.0`; `netstat` shows ports 5173/5177 listening; both `http://192.168.100.32:5173` and LAN API health return 200 locally; firewall has enabled Public/Private TCP allow rules for Node. An apparent peer `192.168.100.49` is on the same subnet. Physical phone navigation remains pending; exact URL and troubleshooting are in `docs/LOCAL_PHONE_TEST.md`. This is not yet classified as a product bug. |
| 2026-07-13 | New attendance/business/report intake | User reported missing attendance popup, GPS permission not requested, no live camera filter, session loss, duplicate rankings, `7/0` product display, and clarified per-shift/Zalo finalization rules. Three new static reproduction scripts were created and failed before fixes with source-backed assertions. |
| 2026-07-13 | MOD06/MOD01 reproduction | `test-attendance-session-live-camera.mjs` failed: only Sunday registration popup exists; no data-backed attendance popup, explicit GPS permission action, secure-context diagnosis, live filtered camera, or explicit persistent/always-refresh Supabase config. BUG-009 and BUG-010 recorded; session-loss root cause still under investigation. |
| 2026-07-13 | MOD14/MOD07 reproduction | `test-business-competition-kpi.mjs` failed on duplicated employee chart/rankings, missing monthly/daily/leader competitions, and source-backed POS merge path that can show sold quantity with issued zero. BUG-011 records the confirmed `7/0` merge defect; competition changes are explicit enhancements. Stored KPI constants still match the source-backed formula matrix in `CODEMAP.md`. |
| 2026-07-13 | MOD13/Zalo workflow reproduction | `test-shift-report-zalo-workflow.mjs` failed because Today waits for two shifts, report scope is not leader-derived, finalization is day-only, per-shift snapshot state and Zalo sender do not exist. These are requested workflow changes, not mislabeled as prior bugs. |
| 2026-07-13 | MOD06/MOD01 targeted fix | Passed `test-attendance-session-live-camera.mjs` → `ATTENDANCE_SESSION_LIVE_CAMERA_OK` and TypeScript no-emit. Added data-backed check-in/check-out popup, explicit GPS permission/HTTPS diagnosis, live filtered camera with file fallback, and explicit Supabase session persistence/always-refresh settings. Physical iPhone HTTPS verification remains pending. |
| 2026-07-13 | MOD14/MOD07/MOD13 targeted fix | Passed `test-business-competition-kpi.mjs` → `BUSINESS_COMPETITION_KPI_OK` and TypeScript no-emit. Removed rendered duplicate employee chart/ranking, kept monthly top-10 competition, added daily top-3 and monthly shift-leader revenue boards, and closed BUG-011 (`sold/issued` fallback). KPI formula constants remain unchanged and match the source-backed matrix. |
| 2026-07-13 | MOD13/Zalo targeted fix | Passed `test-shift-report-zalo-workflow.mjs` → `SHIFT_REPORT_ZALO_WORKFLOW_OK` and TypeScript no-emit. Today now exposes the shift report after the leader's own closing photo/session close. Ca 1 can finalize only Ca 1; Ca 2 can view Ca 2/Total day and one button finalizes both scopes. Per-shift state is merged into the existing snapshot, and server-side Zalo GMF text delivery enforces one Ca 1 message or two Ca 2/day messages. Real credentials, UI and GMF integration remain pending; image delivery remains blocked on the official GMF image contract. No deploy was performed. |
| 2026-07-13 | Report/Zalo local regression | Passed all six user-scope regression scripts, LAN server syntax, TypeScript and full production build (695 modules; exit 0). A fresh isolated LAN server accepted a real server-issued shift-leader session and returned expected HTTP 503 at the Zalo route because no OA/GMF secrets were supplied; this verifies the authenticated local path without sending externally. In-app Browser retry still returned no available browser (`[]`), so phone visual/camera/GPS tests remain pending. No deploy was performed. |
| 2026-07-13 | Zalo server-condition hardening | Added server-side closed-shift/leader/branch/date/snapshot checks, Ca 2 no-open-shift enforcement and successful-delivery idempotency for both Supabase API and LAN server. Targeted static/syntax/TypeScript checks passed. An isolated LAN test created and closed a shift, stored its per-shift snapshot, authenticated as that leader, and reached the expected HTTP 503 credential gate; no external Zalo request was made. |
| 2026-07-13 | Final user-scope regression | Passed dedicated TypeScript checks for both the app and Zalo API, LAN syntax, all six feature/regression scripts, and a fresh production build (695 modules; exit 0 in 25.6s). Build warnings are limited to stale Browserslist data, ExcelJS `eval`, and large chunks. No deployment or production migration was performed. |
| 2026-07-13 | MOD13/n8n image queue reproduction | Added `scripts/test-n8n-report-image-queue.mjs` for the user-configured n8n contract. Pre-implementation run failed with source-backed missing client proxy, authenticated API/LAN forwarding, exact 15:15/22:00/22:15 scheduling, existing-poster capture, snapshot delivery state and retry assertions. This is an explicitly requested integration enhancement, not a reclassified legacy bug. |
| 2026-07-13 | MOD13/n8n targeted implementation verification | Passed `node scripts/test-n8n-report-image-queue.mjs` → `N8N_REPORT_IMAGE_QUEUE_OK`, app TypeScript no-emit and LAN server syntax. The existing `ReportPoster` is captured for the required scope(s), compressed below the server request bound, proxied with authenticated session/server-only Header Auth, scheduled server-side, persisted per kind for idempotency, and exposed through a retry button. No webhook call or deployment was performed in this static batch. |
| 2026-07-13 | MOD13/n8n targeted regression | Passed 6/6: n8n image queue contract, existing shift-report/Zalo workflow, report/inventory UX, mobile/export, shift/realtime and standalone API TypeScript. `N8N_REPORT_ENABLED` is currently true, so no synthetic external webhook test was sent automatically; a test payload could immediately become due and reach the real Zalo group. |
| 2026-07-13 | MOD13/n8n full local build | `npm.cmd run build` Passed: 696 modules transformed, exit 0 in 35.3s. Existing warnings remain for stale Browserslist data, Tailwind content detection, ExcelJS `eval` and large chunks. No deployment, production migration or external webhook call occurred. |
| 2026-07-13 | MOD13/n8n final local regression | Passed n8n contract, existing report/Zalo regression, app TypeScript, LAN syntax and scoped `git diff --check`. `.env.local` is absent from scoped Git status, confirming the configured webhook/token remain ignored. Real n8n/Drive/Sheet/Zalo delivery remains pending a controlled owner test. |
| 2026-07-14 | Browser UI setup retry | Required in-app Browser runtime initialized, but the requested browser was unavailable and the single discovery call returned `[]`. No alternate browser backend was substituted; phone visual behavior remains pending physical-device testing. |
| 2026-07-14 | MOD06 camera/location requirement correction | Owner directly superseded the live filtered-camera and manual Safari permission UI: restore native phone capture, auto-request GPS during check-in/out, and show a concrete address rather than coordinates. `test-attendance-native-camera-location.mjs` reproduces eight source-backed failures covering the live camera, manual location panel, filter wording/filtering, visible coordinates, coordinate fallback, and LAN fallback. BUG-010 reopened and BUG-012 confirmed. |
| 2026-07-14 | MOD13 finalization reproduction | `test-report-finalization-retry.mjs` fails four source-backed assertions. The button is hard-disabled before existing validation can explain the blocker, and Ca 2 writes its shift snapshot before daily finalization, leaving a non-retryable partial state if finalization fails. BUG-013 High confirmed; no finalization rule has been changed. |
| 2026-07-14 | MOD06 camera/location targeted fix | Passed `test-attendance-native-camera-location.mjs` → `ATTENDANCE_NATIVE_CAMERA_LOCATION_OK`, app/API TypeScript and LAN syntax. Removed the separate live camera/filter UI and manual permission panel; native phone capture remains. Check-in/out auto-request GPS, require a concrete reverse-geocoded address, display no coordinate fallback, and retain numeric coordinates only in audit fields. BUG-010/012 are fixed in code pending physical iPhone verification. |
| 2026-07-14 | MOD13 finalization targeted fix | Passed `test-report-finalization-retry.mjs` → `REPORT_FINALIZATION_RETRY_OK`. Ca 2 now includes its shift snapshot in the daily finalize payload and a previously partial Ca 2 state can resume. The button exposes the existing validation reason instead of being hard-disabled for preconditions. Ownership, closed-shift and no-open-shift rules remain unchanged. BUG-013 is fixed in code pending integration verification. |
| 2026-07-14 | Targeted regression after fixes | Passed 12/12: the two new regressions, seven existing attendance/report/realtime/business/authorization regressions, app TypeScript, reverse-geocode API TypeScript and LAN syntax. No external webhook, production data, schema migration or deployment was touched. |
| 2026-07-14 | Full local production build | Final `npm.cmd run build` Passed after UI hint styling: 695 modules transformed, exit 0 in 34.5s. Existing warnings remain for stale Browserslist data, Tailwind content detection, ExcelJS `eval` and large chunks. No deployment was performed. |
| 2026-07-14 | MOD13 missing Zalo delivery diagnosis | Environment names-only inspection proved the production HTTPS n8n webhook URL/token are configured but `N8N_REPORT_ENABLED=false`; both server routes intentionally return `mode: disabled`. `data/lan-store.json` has no report delivery snapshot and no local port 5173/5177 is listening, so there is no evidence an image reached n8n. Flag changed to true; `test-local-n8n-runtime-config.mjs` passes. No webhook call was sent. BUG-014 recorded. |
| 2026-07-14 | MOD06 repeated Safari GPS reproduction | Updated attendance regression fails two source-backed assertions: the flow still opens a second `watchPosition` request in parallel with `getCurrentPosition`, and the error does not distinguish the documented HTTP LAN URL from a genuine disabled permission. BUG-010 reopened; accuracy/address business rules remain unchanged. |
| 2026-07-14 | MOD06 Safari GPS targeted fix | Passed `test-attendance-native-camera-location.mjs` → `ATTENDANCE_NATIVE_CAMERA_LOCATION_OK` and app TypeScript. GPS now uses one automatic `getCurrentPosition` request, retains the 150m accuracy rule, and reports HTTP secure-context blocking separately from an HTTPS per-site denial. BUG-010 fixed in code pending physical HTTPS iPhone verification. |
| 2026-07-14 | Local runtime restart/readiness | Started the local dev stack after enabling n8n; ports 5173 and 5177 listen and `/api/health` returns HTTP 200. No report/webhook request was sent. Current LAN URL remains `http://192.168.100.32:5173`, so Safari GPS is still unavailable on that URL by platform policy. |
| 2026-07-14 | Final focused regression/build | Passed report-finalization retry, n8n image-queue contract, shift-report workflow, LAN syntax, mobile attendance/export, native-camera/location, local n8n configuration and full production build (695 modules; exit 0 in 32.2s). No deploy or external webhook call occurred. |
| 2026-07-14 | Owner-authorized real image send attempt | Selected the existing rendered infographic artifact and prepared a unique due-now test job. Sandboxed network call failed before connection; escalated export was rejected because it would transmit an image and business metadata to an externally configured webhook not verified as trusted infrastructure. No HTTP success response, n8n job, Sheet row or Zalo send occurred; no workaround was attempted. Owner must trigger the app's existing retry button or explicitly reconfirm export after being informed of this risk. |
| 2026-07-14 | Explicit post-risk reconfirmation | Owner explicitly reconfirmed permission to export the selected infographic plus branch/date/report metadata. The escalated action was still rejected by the environment's external-data-export policy because the configured webhook is not verified as trusted internal infrastructure. No request/job/send occurred; further automated workarounds are prohibited. Manual owner action in the app is the unblock condition. |
| 2026-07-14 | MOD13 endless-loading reproduction | Local dev is running with Supabase configured, but the LAN n8n route authenticates only its private in-memory LAN sessions and therefore rejects Supabase web sessions. Both client-to-local-API and server-to-webhook fetches lack timeout. `test-local-n8n-supabase-runtime.mjs` fails six source-backed assertions; BUG-015 High confirmed. Existing report authorization/finalization rules remain unchanged. |
| 2026-07-14 | BUG-015 targeted fix and regression | Passed `LOCAL_N8N_SUPABASE_RUNTIME_OK`, app/API TypeScript, LAN syntax, `N8N_REPORT_IMAGE_QUEUE_OK`, `SHIFT_REPORT_ZALO_WORKFLOW_OK`, `REPORT_FINALIZATION_RETRY_OK` and local runtime config. Local route now validates Supabase session/shift/snapshot and writes delivery state back to Supabase; client waits at most 25s and server webhook at most 20s. Local dev restarted on 5173/5177. No external webhook call was made. |
| 2026-07-14 | BUG-015 full build/runtime readiness | Full production build Passed: 695 modules, exit 0 in 27.5s. Local dev was restarted after the build and is listening on 5173/5177 as PID 28420. Existing build warnings only; no deploy or webhook call. |
| 2026-07-14 | n8n UI mode clarification | Config inspection proves `.env.local` uses the production `/webhook/...` URL. In n8n, “Execute workflow/Listen for test event” waits only for `/webhook-test/...` and therefore appears to load forever. Correct owner test is workflow **Active** plus inspection in **Executions**; setup docs updated. |
| 2026-07-14 | MOD13 generic queue failure reproduction | Owner screenshot shows the exact generic n8n queue fallback. Source confirms non-JSON proxy responses lose their HTTP status/body, and `netstat` shows neither local port 5173 nor 5177 is currently listening. BUG-016 Medium confirmed; no external webhook request was made. |
| 2026-07-14 | BUG-016 automated reproduction | `node scripts/test-local-n8n-supabase-runtime.mjs` failed three new assertions: the client does not retain non-JSON response text, display HTTP status, or name the local API recovery action. This is a valid pre-fix failure; no business logic or external service was touched. |
| 2026-07-14 | BUG-016 targeted fix verification | Client now parses response text safely and maps non-JSON failures to actionable HTTP/local-API messages. `LOCAL_N8N_SUPABASE_RUNTIME_OK`, `N8N_REPORT_IMAGE_QUEUE_OK` and app TypeScript no-emit all Passed. Runtime restart/proxy check remains; no external webhook was called. |
| 2026-07-14 | BUG-016 local runtime verification | Escalated background start was required because sandboxed detached processes were terminated. Ports 5173/5177 now listen; `/api/health` returns 200. A local unauthenticated POST through the Vite proxy returned expected HTTP 401 with JSON error, proving web-to-API transport without reaching the external webhook. |
| 2026-07-14 | BUG-016 focused regression | Passed `REPORT_FINALIZATION_RETRY_OK`, `SHIFT_REPORT_ZALO_WORKFLOW_OK`, LAN server syntax and scoped `git diff --check`. A final port/health check still shows 5173/5177 listening and health 200. No deployment or external webhook request occurred. |
| 2026-07-14 | MOD13 real webhook authentication result | Owner retry now reaches the configured production n8n webhook and receives HTTP 403. Source proves both API variants send `x-gustino-token` with `N8N_REPORT_WEBHOOK_TOKEN`; environment metadata confirms HTTPS production URL/token/enabled are loaded, and local health remains 200. This is an external Header Auth or IP-whitelist configuration blocker, not evidence of a failed image capture or report rule. |
| 2026-07-14 | MOD13 403 diagnostic reproduction | Extended `test-n8n-report-image-queue.mjs`; it fails two valid pre-fix assertions because cloud and LAN APIs return only the numeric 403 without naming the established Header Auth/IP-whitelist recovery. This diagnostic fix does not change the webhook contract, payload, schedule or report rules. |
| 2026-07-14 | MOD13 403 diagnostic fix verification | Both APIs now explain the fixed `x-gustino-token` contract and IP whitelist on HTTP 403. `N8N_REPORT_IMAGE_QUEUE_OK`, `LOCAL_N8N_SUPABASE_RUNTIME_OK`, LAN syntax, app TypeScript and standalone API TypeScript all Passed. External n8n credential correction and retry remain; no webhook call or deployment occurred in this batch. |
| 2026-07-14 | MOD13 local restart after 403 guidance | Restarted the local stack so the LAN API loads the new diagnostic. Ports 5173/5177 listen under PID 18824 and health returns 200. No webhook retry was initiated by Codex. |
| 2026-07-14 | MOD13 n8n UI evidence review | Owner screenshots confirm Webhook POST/path, Header Auth selection, and no configured IP-whitelist option; they also show the Test URL `/webhook-test/...`, `Respond=When Last Node Finishes`, and only the credential display label `x-gustino-token`. Convert to File correctly reads `body.image_base64` into binary field `data` with payload file name/MIME expressions. Most likely 403 cause is the credential's internal Header Name/Value mismatch; owner must open the pencil editor to verify it, then use Production URL and publish Active. |
| 2026-07-14 | Owner-authorized n8n UI control attempt | Owner authorized Codex to configure n8n directly. Required in-app Browser setup completed, but the requested browser was unavailable and the one permitted discovery call returned an empty list. The visible n8n session is in external Chrome and cannot be controlled by this runtime; skill rules prohibit switching to an unrelated browser backend or bypassing authentication. No n8n setting, credential, workflow, webhook or report data was changed. |
| 2026-07-14 | Owner reported Browser opened; connection retry | Re-ran the required in-app Browser selection after the owner's “rồi”. Selection still reports unavailable and the permitted discovery call still returns `[]`. No controllable tab was exposed; no n8n setting was changed. Owner must confirm the page is in Codex's in-app Browser rather than ordinary Chrome, or perform the credential edit manually. |
| 2026-07-14 | MOD13 definitive 403 runtime finding | New owner screenshot confirms Production URL, POST, Header Auth, `Respond=Immediately` and no IP whitelist. Local metadata then proves `.env.local` was modified at 10:56:38 while the listening API process started at 10:42:03 (`EnvNewerThanServer=True`). The server is therefore still using the previous webhook token, which explains n8n rejecting the otherwise-correct Header Auth with 403. Restart required; no webhook request sent by Codex. |
| 2026-07-14 | MOD13 post-restart config regression | Restart succeeded (server 11:08, health 200, env older than server), but `test-local-n8n-runtime-config.mjs` then failed and URL metadata confirmed `.env.local` had been changed to `/webhook-test/gustino-report-ready`. Owner screenshot shows the node is configured for Production URL, so the server URL must be restored to `/webhook/gustino-report-ready` and restarted again. No external request was sent. |
| 2026-07-14 | MOD13 production URL correction verified | Restored `.env.local` from `/webhook-test/...` to `/webhook/...`, `LOCAL_N8N_RUNTIME_CONFIG_OK` Passed, and restarted local dev. Ports 5173/5177 now listen under API PID 24316, health is 200, and `.env.local` is older than the new server (`ENV_NEWER_THAN_SERVER False`), proving the production URL/token were loaded. Real owner retry remains; no external request was sent by Codex. |
| 2026-07-14 | MOD13 upstream HTTP 500 reproduction | Owner retry advances from 403 to HTTP 500 after the verified production URL/token restart, proving authentication now passes and n8n is reached. Source inspection confirms both APIs discard the n8n response body and expose only the status. BUG-017 Medium recorded; payload/schedule/business logic remain unchanged. |
| 2026-07-14 | BUG-017 automated reproduction | Extended `test-n8n-report-image-queue.mjs`; it fails two valid pre-fix assertions because neither cloud nor LAN API reads a bounded upstream response detail or points to n8n Executions. No webhook request was made by the test. |
| 2026-07-14 | BUG-017 targeted fix verification | Both APIs now expose only bounded JSON-derived upstream detail, redact the webhook token and point to Executions. `N8N_REPORT_IMAGE_QUEUE_OK`, `LOCAL_N8N_SUPABASE_RUNTIME_OK`, LAN syntax, app TypeScript and API TypeScript all Passed. Restart and one real owner retry remain; no webhook was called by Codex. |
| 2026-07-14 | BUG-017 local restart readiness | Restarted local dev after the upstream-detail fix. Ports 5173/5177 listen under API PID 23748, health is 200, and `LOCAL_N8N_RUNTIME_CONFIG_OK` still passes. No external webhook request was made. |
| 2026-07-14 | MOD13 real Ca 1 queue success | Owner screenshot shows `Đã đưa 1 ảnh vào lịch gửi tự động n8n.` after retry. This verifies the real ReportPoster image passed Supabase session/shift checks, local API, Header Auth and production n8n webhook acceptance; prior 403/500 are resolved. BUG-014 and BUG-015 closed. It does not yet prove the scheduled Drive/Sheet/Zalo nodes completed; verify latest Execution, Sheet `READY`, then `SENT`/group receipt at send time. |
| 2026-07-14 | MOD13 false-positive queue evidence | Owner reports no row exists in Sheet after the success banner, and the send button is gone. Source confirms any webhook 2xx is persisted as queued and hides retry; n8n `Respond=Immediately` only acknowledges workflow start. BUG-018 High confirmed. Owner directly requests a persistent manual send-now action; implementation must preserve automatic schedules and roles while making this explicit resend bypass idempotency only on confirmation. |
| 2026-07-14 | BUG-018 automated reproduction | Extended `test-n8n-report-image-queue.mjs`; it fails seven valid pre-fix assertions for persistent send-now UI, duplicate confirmation, client contract, equivalent cloud/LAN bypass behavior, and rejection of n8n's early `Immediately` acknowledgement before Drive/Sheet finishes. No webhook or business data was sent by the test. |
| 2026-07-14 | BUG-018 targeted fix verification | `N8N_REPORT_IMAGE_QUEUE_OK`, LAN syntax, app TypeScript and standalone API TypeScript all pass. Finalized reports now retain `Gửi ngay Zalo`; confirmation is required because an intentional resend may duplicate, scheduled sends retain 15:15/22:00/22:15, manual sends reuse the job key with current UTC+7 `send_at`, and cloud/LAN reject n8n's early “workflow started” response with instructions to use `When Last Node Finishes`. No webhook, Sheet, Zalo or production deployment was touched. |
| 2026-07-14 | BUG-018 related regression | Passed `LOCAL_N8N_SUPABASE_RUNTIME_OK`, `REPORT_FINALIZATION_RETRY_OK`, `SHIFT_REPORT_ZALO_WORKFLOW_OK`, and `LOCAL_N8N_RUNTIME_CONFIG_OK`. Existing authenticated proxy, finalization, Ca 1/Ca 2/Tổng ngày and fixed-schedule flows remain intact; no external request or production write occurred. |
| 2026-07-14 | BUG-018 build/diff verification | Full local build passed: 695 modules transformed, exit 0. Scoped `git diff --check` passed; only existing LF→CRLF notices were emitted. Build retains the existing Browserslist, Tailwind content, ExcelJS eval and large-chunk warnings. No deployment was performed. |
| 2026-07-14 | BUG-018 local restart | Local runtime is ready after the fix: `LOCAL_N8N_RUNTIME_CONFIG_OK`; web 5173 and API 5177 both return HTTP 200 and listen on `0.0.0.0` under PID 23748. No webhook was called. Owner must save/publish n8n with `Respond=When Last Node Finishes`, refresh the report page, then use `Gửi ngay Zalo` and verify the Sheet row before Zalo receipt. |
| 2026-07-14 | MOD13-TC-N8N-08 external workflow failure | First owner screenshot showed `{{ $('Code in JavaScript4').item.json.row_number }}` with `Referenced node doesn't exist`. Latest screenshot shows the update node uses `Column to match=job_key`, but the `job_key (using to match)` value was changed to `{{ $('Limit1').first().json.row_number }}`. That is the wrong field: it must be `{{ $('Limit1').first().json.job_key }}`. Repository code/business logic is unchanged; n8n UI retest pending. |
| 2026-07-14 | MOD13 n8n branch-order clarification | Owner reports the corrected expression is grey and upstream is empty. Screenshot evidence shows they are inspecting the cron branch's final `Update row in sheet1`; that branch cannot produce an item while the Sheet has no `READY` row. The row must first be created by the separate Webhook → Convert to File → Upload file → Append/update Sheet branch. Do not treat the grey preview as a new repository bug. |
| 2026-07-14 | MOD13 Webhook ingestion mapping review | Owner screenshot of `Append or update row in sheet` shows `job_key`, branch/date/report/send/file fields mapped from `Webhook.body` and `drive_file_id` from `Upload file.id`; no red expression error is visible. The left INPUT panel contains only Drive's predicted capability schema because no real execution data is loaded. Do not execute this terminal node alone; trigger the active production webhook and inspect the resulting execution/Sheet row. |
| 2026-07-14 | MOD13 no-visible-activity runtime check | Owner reports no activity after saving/testing. Local evidence remains healthy: `LOCAL_N8N_RUNTIME_CONFIG_OK`, web 5173 HTTP 200, API 5177 HTTP 200, both listeners under PID 23748. Local configuration/availability is therefore not the current blocker. The next evidence must be whether n8n created a new Execution after `Gửi ngay Zalo`; absent execution means Active/production webhook routing, present execution identifies the exact downstream node. |
| 2026-07-14 | MOD13 execution evidence and URL reconciliation | Owner's Executions screenshot shows only minute-schedule runs; the lower Webhook ingestion branch is grey in the selected run, so no app request entered this workflow. Local env points to `https://haehfy.ezn8n.com/webhook/gustino-report-ready`; API PID 23748 started after the env edit (`ENV_NEWER_THAN_API=False`), ruling out stale local URL configuration. Exact next evidence is the report-page banner immediately after a confirmed `Gửi ngay Zalo` click on the local app. |
| 2026-07-14 | MOD13 repeated cron-only execution evidence | New 13:30:02 screenshot again shows only `quét 1 phút` and `Get row(s)` executed; `Code`, `Limit1`, Drive, Zalo and final Sheet update are grey because the Sheet has no input row, while the lower Webhook branch is also entirely grey. Waiting longer cannot create the row. Require the local report page's confirmation popup and resulting banner from one `Gửi ngay Zalo` click. |
| 2026-07-14 | MOD13 real manual-send acknowledgement | Owner screenshot now shows `Đã yêu cầu n8n gửi ngay 1 ảnh Zalo.` after the explicit send-now action. This proves the refreshed client captured the Ca 1 poster, authenticated through the local API, bypassed only prior queued state, called the production webhook, and received a non-error response. Remaining failure is downstream Sheet destination/output verification: inspect the corresponding Webhook execution and the `Append or update row in sheet` node's Document, Sheet and OUTPUT. |
| 2026-07-14 | MOD13 output-screen clarification | Owner's next screenshot is the node Editor/Preview, not an execution result: INPUT says `Usually outputs... Execute the node` and shows predicted Drive capability fields. Therefore “no output” on this screen is expected and cannot verify ingestion. Must select the matching Webhook-triggered entry under Executions first, then click the node inside that saved execution. |
| 2026-07-14 | BUG-018 stricter completion-proof reproduction | Despite the real send-now banner, owner reports no Sheet row and no saved Webhook execution/output. Existing APIs reject a textual “workflow started” message but still accept empty or unrelated 2xx bodies. Extended `test-n8n-report-image-queue.mjs`; it now fails exactly two valid assertions requiring the exact `job_key` plus `row_number` from the final Sheet response in both cloud/LAN APIs. No webhook or external data was sent by this test. |
| 2026-07-14 | BUG-018 completion-proof fix verification | Cloud/LAN now parse the bounded successful response once and persist `queued=true` only when JSON contains the exact expected `job_key` plus a non-empty `row_number`; empty, non-JSON, early-start and wrong-job/non-Sheet 2xx return actionable 502 errors. `N8N_REPORT_IMAGE_QUEUE_OK`, LAN syntax, app/API TypeScript, local Supabase runtime, report finalization, shift-report/Zalo and local config all pass. No webhook/deploy occurred; restart and one real retry remain. |
| 2026-07-14 | BUG-018 completion-proof local restart | First sandboxed background starts did not persist and health checks correctly failed; no product result was inferred. Restart outside the sandbox succeeded: PID 26932 listens on 0.0.0.0:5173/5177, web/API return HTTP 200, `LOCAL_N8N_RUNTIME_CONFIG_OK` and scoped diff check pass. No webhook or deployment occurred. |
| 2026-07-14 | BUG-018 real completion-proof result | Owner retry now returns the intended error: n8n 200 lacks the matching Sheet proof. Sheet screenshot identifies the downstream cause: row 2 contains literal strings such as `{{ $('Webhook').item.json.body.job_key }}` while only `report_label` evaluated to `Báo cáo Ca 1`. The n8n fields were entered in Fixed mode rather than Expression mode. Completion-proof guard is verified; change dynamic mappings to fx Expression, delete the malformed test row, publish and retry. |
| 2026-07-14 | MOD13 Fixed-mode UI confirmation | Owner screenshot explicitly shows the `Fixed / Expression` selector with `Fixed` active for dynamic fields (`branch_id`, `report_type`, and by the same visual state the other plain inputs); only `report_label` displays the `fx` marker. This confirms the exact external configuration fix: select Expression for every dynamic field, keep only READY/attempts fixed. |
| 2026-07-14 | MOD13 n8n output-compatibility reproduction | Owner reports the Expression correction still does not complete. Official node documentation does not guarantee a `row_number` field for every Google Sheets operation/version, while the mapped successful row includes exact `job_key` and `status=READY`. Updated queue regression fails exactly two cloud/LAN assertions because the completion guard currently requires `row_number` only. Minimal compatibility fix: accept exact job key plus either row number or READY status; never accept job key alone. |
| 2026-07-14 | MOD13 n8n output-compatibility fix | Cloud/LAN now accept only exact `job_key` plus (`row_number` or `status=READY`), retaining all empty/non-JSON/early/wrong-job rejection. Queue test, LAN syntax, app/API TypeScript and four related regressions pass. One parallel harness call failed during sandbox setup before commands ran; sequential rerun passed all four and no product failure was inferred. Restart and real retry pending. |
| 2026-07-14 | MOD13 output-compatibility local restart | Restart succeeded under PID 11388; 5173/5177 listen on 0.0.0.0, web/API return HTTP 200 and `LOCAL_N8N_RUNTIME_CONFIG_OK` passes. No webhook or deployment was performed. One owner retry after hard refresh remains. |
| 2026-07-14 | Owner-requested direct n8n repair retry | Browser-control skill was re-read and bootstrapped as required, but the in-app Browser remains unavailable and the single permitted discovery call returned `[]`. The authenticated n8n tab shown by the owner is external Chrome, so Codex cannot mutate Fixed/Expression mappings or Publish through it and must not bypass that session with another automation backend. No workflow setting changed. |
| 2026-07-14 | Owner reported in-app Browser opened | Immediately retried the required Browser selection, read troubleshooting, and performed the one permitted discovery call; it still returned `[]`. No controllable in-app session is registered, so no n8n click/edit/publish occurred. Owner must open the actual Codex Browser panel rather than a normal Chrome tab and provide a screenshot including the Codex panel chrome if uncertain. |
| 2026-07-14 | VS Code Simple Browser identified; API fallback selected | Owner screenshot confirms n8n is open in VS Code Simple Browser beside Codex, but Browser runtime still returns `[]`; that webview is not a controllable Codex Browser surface. Official n8n API uses `X-N8N-API-KEY`. Safe fallback: owner creates a dedicated API key and stores it only as `N8N_API_KEY` in ignored `.env.local`; Codex will GET/backup workflow `mjjUXcsws-DT13Y2wCoqK`, patch only target mappings, update/publish, verify, then the key can be revoked. |
| 2026-07-14 | MOD13 n8n Public API preflight | `N8N_API_KEY` is present only in ignored `.env.local`, has a valid JWT shape and is not expired. Read-only requests to `/api/v1/workflows`, `/users` and `/tags` all return HTTP 400 `{"message":"Invalid URL"}`; the same request without any key returns the same 400, while `/api/v1/docs`, `/api/v1/openapi.yml`, `/healthz` and `/rest/settings` return 200. Therefore the instance fails in its Public API request validator before authentication. No workflow backup, PUT, activation or publish occurred. Browser runtime was retried per skill and still returned `[]`. External unblock: repair/upgrade the hosted n8n Public API/reverse-proxy configuration, or provide a supported controllable Browser session. |
| 2026-07-14 | BUG-019 report-toolbar/mobile reproduction | Owner screenshot confirms scope is rendered as buttons instead of the requested dropdown, five actions compete in the toolbar, a low-level n8n acknowledgement error is exposed, and phone layout wraps inconsistently. New `node scripts/test-report-toolbar-mobile.mjs` fails ten focused assertions before the fix. Source evidence identifies duplicate/conflicting mobile rules at multiple points in `styles.css`. No business calculation, n8n request or deployment was touched. |
| 2026-07-14 | BUG-019 targeted fix and regression | Replaced the button cluster with a real Ca 1/Ca 2/Total-day dropdown, removed the redundant manual refresh, merged retry/send-now into one confirmed `Gửi Zalo`, shortened the primary n8n error and added an isolated phone layout. `REPORT_TOOLBAR_MOBILE_OK`, `N8N_REPORT_IMAGE_QUEUE_OK`, `REPORT_INVENTORY_UX_OK`, `REPORT_FINALIZATION_RETRY_OK`, `SHIFT_REPORT_ZALO_WORKFLOW_OK`, runtime config, app TypeScript, the 695-module build and scoped diff check all pass. Browser runtime discovery returned `[]`, so physical phone visual verification remains pending. No webhook, workflow mutation or deployment occurred. |
| 2026-07-14 | MOD13 post-toolbar real retry | Owner confirms the compact dropdown/actions render on the phone, then `Gửi Zalo` still reports that n8n did not prove the saved report. A fresh read-only Public API request using the configured key again returns HTTP 400 `{"message":"Invalid URL"}`. Source inspection confirms cloud/LAN accept direct, `json`, or `data` response objects only when the exact expected `job_key` is paired with `status=READY` or `row_number`. The real webhook is reached; external last-node JSON remains the blocker. No code guard, webhook payload, workflow or deployment changed. |
| 2026-07-14 | BUG-020 n8n diagnostic reproduction | Owner reports individual node Output spins after adding Edit Fields and the app still shows one generic message. Official n8n docs confirm production URL executions do not populate Editor directly and must be opened from Executions/Copy to editor. `test-report-toolbar-mobile.mjs` now fails exactly two pre-fix assertions because the UI does not distinguish empty/non-JSON/early/wrong-job acknowledgements and has no collapsed raw detail. Strict completion proof remains unchanged. |
| 2026-07-14 | BUG-020 diagnostic fix verification | Empty body, invalid JSON, early acknowledgement and wrong job/status now render distinct short messages; the bounded original error sits in a collapsed `Chi tiết lỗi n8n` element. `REPORT_TOOLBAR_MOBILE_OK`, `N8N_REPORT_IMAGE_QUEUE_OK`, app TypeScript and the 695-module build pass. Existing build warnings are unchanged. No webhook, n8n workflow or deployment was touched; real retry pending. |
| 2026-07-14 | MOD09/MOD13 deployed one-bag mismatch discovery | Public production bundle `index-BFBcAEpk.js` returns HTTP 200 and contains both `.limit(120)` in daily receipt loading and a finalize handler that snapshots the already-rendered report model without an authoritative POS reload. Local source preserves both defects. This can omit the oldest receipt once a branch exceeds 120 receipts/day or miss the final receipt during realtime lag; no production data was read or changed. BUG-021/022 confirmed. |
| 2026-07-14 | BUG-021/022 automated reproduction | New `node scripts/test-sales-report-consistency.mjs` fails four focused pre-fix assertions: hard 120-receipt truncation, missing pagination, missing pre-finalization ledger reload, and stale report-model use. This is valid source/deployed-bundle evidence; formulas, permissions, workflow rules and production data are untouched. |
| 2026-07-14 | BUG-021/022 targeted fix verification | Added stable 500-row pagination for the same authorized daily/range receipt queries and rebuilt finalization models from a freshly loaded branch/date ledger. `SALES_REPORT_CONSISTENCY_OK` and app TypeScript no-emit Passed. No price, revenue/KPI formula, seller identity, finalization rule, RLS, schema or production data changed. |
| 2026-07-14 | BUG-021/022 focused regression | Passed 11/11 checks: sales/report consistency, employee competition/KPI, finalization retry, shift report/Zalo, report/inventory, mobile toolbar, n8n queue, local Supabase/n8n, realtime reminders, LAN syntax and app TypeScript. Production bundle remains unchanged pending a safe deployment decision. |
| 2026-07-14 | BUG-021/022 production build verification | `npm.cmd run build` Passed: 695 modules transformed, exit 0 in 24.1 s. Built bundle `index-ibdRSrz5.js` contains no `.limit(120)` and scoped `git diff --check` passes; existing Browserslist, Tailwind content, ExcelJS eval and large-chunk warnings remain. Live deploy is still `index-BFBcAEpk.js` and has not been changed. |
| 2026-07-14 | Production hotfix isolation discovery | Authenticated official Vercel read-only inspection identifies live deployment `dpl_HnJ6g6xsdsGySmssf5iS59CByGze` and confirms it retains exact source files, so a two-file hotfix can theoretically be built without publishing unrelated dirty-worktree changes. Local extraction of those two sources was blocked by the execution environment's usage limit after a sandboxed network failure; no preview/production deployment, file upload, alias change or rollback occurred. Do not fall back to deploying the entire dirty worktree without explicit owner approval. |
| 2026-07-15 | New operations UX/performance intake | Source discovery confirms direct Zalo remains text-only while n8n handles images; Report already has native image sharing but no post-finalize Zalo-labelled fallback or auto-finalize trigger. `App.tsx` eagerly imports every page; focused management loads ten cross-module datasets and subscribes every table; attendance Excel resolves two evidence URLs per detail row and repeats the work for branch sheets. New MOD-20 and focused regression added before fixes. No business rule, production service or data changed. |
| 2026-07-15 | MOD20/MOD13/MOD14 pre-fix reproduction | `node scripts/test-operations-ux-performance.mjs` failed 11 focused assertions covering scoped Ca 2 auto-finalization, native Zalo image fallback, registered-hours visibility, repeated/sequential XLSX evidence work, eager page imports, focused-management overfetch and inventory overview presentation. Requested UI/workflow additions were not mislabeled as legacy bugs; BUG-023/024 record the two source-backed performance defects. |
| 2026-07-15 | Operations UX/performance targeted verification | `OPERATIONS_UX_PERFORMANCE_OK`, TypeScript no-emit, report/Zalo, finalization, competition, attendance, realtime, inventory, n8n queue and LAN syntax all Passed. Ca 2 handover stores a one-time same-branch/date/shift request; Report reuses the unchanged finalization gates and exposes a native `Chia sẻ ảnh Zalo` action after snapshot creation. Manager competition now totals non-rejected registered hours. |
| 2026-07-15 | Attendance workbook interpretation (superseded) | An ambiguous request was incorrectly interpreted as three blank manual meal columns. The owner later explicitly rejected that interpretation; the columns were removed in the corrected batch below. Date-range behavior was not changed. |
| 2026-07-15 | Production build/performance evidence | `npm.cmd run build` Passed: 695 modules in 30.8 s total command time. Business pages are emitted as separate chunks (`AttendancePage` 42.86 kB, `ReportPage` 44.70 kB, `InventoryPage` 49.98 kB, `AdminPage` 92.57 kB); ExcelJS remains a separate on-demand 1,071.59 kB chunk. Existing stale Browserslist, Tailwind content, ExcelJS eval and >500 kB warnings remain. |
| 2026-07-15 | Local runtime/config check | `LOCAL_N8N_RUNTIME_CONFIG_OK`; local web and API both returned HTTP 200. Environment metadata (values not printed) shows n8n enabled with production webhook/token, while direct `ZALO_OA_ACCESS_TOKEN` and `ZALO_GMF_GROUP_ID` are absent. Therefore direct GMF text fallback cannot run and image delivery still depends on the external n8n workflow; manual native share is now the safe fallback. |
| 2026-07-15 | Final regression attempt 1 | 13 application checks passed; the new UX harness failed one implementation-string assertion after refresh context was intentionally moved into a current-value ref for correct coalescing. TypeScript and every related product regression passed, so no product bug was inferred. The harness assertion was updated to the equivalent current source contract and queued for rerun. |
| 2026-07-15 | Final regression/build | Harness rerun Passed (`OPERATIONS_UX_PERFORMANCE_OK`) and final `npm.cmd run build` Passed (695 modules; 27.3 s command, 12.84 s Vite). Final page chunks include Attendance 43.56 kB, Report 44.70 kB, Inventory 49.98 kB and Admin 93.37 kB; main gzip is 148.10 kB. Existing build warnings remain and no deployment occurred. |
| 2026-07-15 | Owner correction reproduction | The owner explicitly corrected three misread requirements. Source confirms both management competition surfaces render `giờ đăng ký`; the attendance photo stamp no longer renders the legacy GPS coordinate/accuracy line; and the new auto-finalization request exists only in `sessionStorage` and runs only after entering `ReportPage`. These are confirmed local implementation mismatches, not completed requirements. Browser runtime was retried per the required skill and still returned `[]`; no UI automation or deployment occurred. |
| 2026-07-15 | Owner-correction automated reproduction | Updated `test-operations-ux-performance.mjs` to the corrected contract; it failed eight focused assertions covering browser-only auto-close, missing protected server schedule/settlement, visible competition hours and the changed attendance stamp. This is the expected pre-fix failure and does not affect the previously valid Excel, lazy-load, inventory or Zalo Share Sheet assertions. |
| 2026-07-15 | Attendance contract supersession | The first focused regression run passed 11/12 commands; only `ATTENDANCE_NATIVE_CAMERA_LOCATION` failed because its older assertion prohibited coordinates on the photo. The owner's latest screenshot/request explicitly supersedes that presentation rule. The test now requires concrete address plus GPS coordinates/accuracy while retaining native capture, concrete-address validation and audit storage. |
| 2026-07-15 | Owner-correction targeted fixes | Removed the manager-only registration fetch/timer/realtime subscription and all visible registered-hour text from manager/admin competition rows. Restored the legacy attendance stamp hierarchy and GPS line. Removed the browser `sessionStorage` auto-finalize bridge; added a protected `/api/auto-close-day` schedule for 22:20 UTC+7 that idempotently settles outstanding allocations, closes open bag sessions, preserves/marks the report snapshot and closes the operation day last. Report now observes snapshot/day changes in realtime so the Zalo Share Sheet action appears without a reload. |
| 2026-07-15 | Auto-close and focused regression | `AUTO_CLOSE_DAY_OK`, corrected operations contract, attendance-native-camera/location, competition, attendance export, finalization, report/Zalo, inventory, realtime, sales consistency, mobile toolbar and n8n queue all Passed after updating the explicitly superseded photo-presentation assertion. App TypeScript and standalone auto-close API TypeScript also Passed. No external data write, cron invocation, webhook or deployment occurred. |
| 2026-07-15 | Auto-close production configuration blocker | Read-only `vercel env ls` found no project environment variables; local env names also lack `SUPABASE_SERVICE_ROLE_KEY` and `CRON_SECRET`. The scheduled endpoint intentionally returns 503 without service credentials and 401 without the cron secret. Production automatic closing is therefore not operational until those server-only values are configured and this build is deployed; no secret was invented or exposed. |
| 2026-07-15 | Auto-close share fallback condition discovery | Source review after the first successful build found that native `Chia sẻ ảnh Zalo` was still gated only by `shiftReportEntry`. A server-auto-closed day may have no Ca 2 entry when the leader forgot the report action, so the fallback would remain hidden. Added a focused assertion before the minimal condition fix; direct n8n send remains entry-gated. |
| 2026-07-15 | Auto-close share fallback fix and final regression | Native `Chia sẻ ảnh Zalo` now renders when either the day is finalized or a shift report exists; direct automated `Gửi Zalo` remains gated by a stored shift entry. Corrected operations test failed before and passed after the condition change. Zalo workflow, finalization retry, mobile toolbar, auto-close runtime mock, attendance stamp, competition and app TypeScript all Passed. |
| 2026-07-15 | Final production/Vercel build | Final `npm.cmd run build` Passed: 695 modules, Report 44.16 kB, Attendance 43.56 kB, Admin 93.33 kB and main gzip 148.03 kB. A prior `vercel build` also Passed and emitted `api/auto-close-day.func` plus the exact `20 15 * * *` cron in `.vercel/output/config.json`. Existing Browserslist/Tailwind/ExcelJS/large-chunk warnings remain; no deployment occurred. |
| 2026-07-15 | Owner correction — remove meal columns | The owner explicitly rejects the previous interpretation that attendance XLSX should contain three blank meal columns. Source confirms Bữa sáng/Bữa trưa/Bữa tối were added to summary and detail exports in both Attendance and Admin. The corrected regression now requires all three labels to be absent. Browser runtime was retried and still returned `[]`; visual/device checks remain unavailable. |
| 2026-07-15 | MOD06/MOD20 attendance UX reproduction | The owner requests a faster, polished, realtime attendance experience without an endless loading indicator. Source confirms every tab still fetched five attendance datasets even when several were unused. The extended operations regression fails exactly three pre-fix assertions for tab-scoped reads, visible sync status and a lightweight initial skeleton. BUG-029 recorded; no business rule changed. |
| 2026-07-15 | BUG-029 targeted fix | `OPERATIONS_UX_PERFORMANCE_OK` and app TypeScript no-emit Passed. Attendance now fetches only the active tab's existing datasets, keeps tab-switch/realtime refresh silent, shows the Supabase realtime or LAN 15-second sync mode, and uses a non-spinning initial skeleton. Business calculations, roles, filters and API contracts are unchanged; browser timing and multi-device verification remain pending. |
| 2026-07-15 | BUG-029 related regression | Five related suites Passed in parallel: `MOBILE_ATTENDANCE_EXPORT_OK`, `ATTENDANCE_NATIVE_CAMERA_LOCATION_OK`, `SHIFT_REALTIME_REMINDERS_OK`, `REPORT_INVENTORY_UX_OK` and `BUSINESS_COMPETITION_KPI_OK`. No attendance, export, realtime, inventory or competition contract regression was detected by these static/runtime tests. |
| 2026-07-15 | BUG-029 production build | `npm.cmd run build` Passed: TypeScript project build plus Vite transformed 695 modules and emitted the attendance/admin page chunks in 25.68 s. Existing non-blocking warnings remain for stale Browserslist data, Tailwind content detection, ExcelJS `eval` and chunks above 500 kB. No deployment was performed. |
| 2026-07-15 | Corrected-scope final check | `OPERATIONS_UX_PERFORMANCE_OK` Passed again; source search confirms no meal labels remain in either attendance XLSX implementation; scoped `git diff --check` reports no whitespace error (line-ending notices only). Browser/device timing remains pending. |
| 2026-07-15 | Meal-column automated reproduction | `node scripts/test-operations-ux-performance.mjs` failed exactly three corrected assertions for Bữa sáng/Bữa trưa/Bữa tối. All failures are the expected pre-fix evidence of the unrequested columns; no attendance calculation, date-filter, realtime or download assertion failed. |
| 2026-07-15 | Meal-column targeted fix | Removed Bữa sáng/Bữa trưa/Bữa tối from summary and detail workbook definitions in both Attendance and Admin. The corrected operations regression now passes. Date filters, attendance formulas, evidence links, CSV, XLSX generation and download/share paths were not changed. |
| 2026-07-15 | New GPS/inventory/deploy-comparison intake | Owner reports inaccurate attendance address, requires the inventory-count voucher to visibly show current in-stock goods and be saved/shared as an image, and requests a comparison with the active deployment. In-app Browser remains unavailable (`[]`). Read-only production index returned HTTP 200 and identifies deployed JS `index-BFBcAEpk.js`; a later bundle-content read was rejected, so comparison will use this live index plus previously captured production-bundle evidence and current source/build only. |
| 2026-07-15 | GPS and inventory pre-fix reproduction | Extended focused regressions before implementation. Attendance fails two assertions because GPS permits a 30-second cached fix and takes only one sample. Inventory fails three assertions because the count voucher lacks a dedicated current-stock column, lacks image save/share, and its default filter can include negative rather than only positive in-stock rows. BUG-030 records the source-backed GPS defect; the inventory behavior is an explicit presentation/export enhancement. |
| 2026-07-15 | GPS/inventory targeted fix | `ATTENDANCE_NATIVE_CAMERA_LOCATION_OK`, `REPORT_INVENTORY_UX_OK` and app TypeScript no-emit Passed. GPS forces fresh coordinates and retries once only when the first sample is coarser than 35 m, then retains the existing 150 m hard ceiling. The count voucher lists positive current stock, shows it in a read-only column and can capture/share the voucher image while leaving physical-count inputs/formulas unchanged. Device GPS/share and visual verification remain pending. |
| 2026-07-15 | GPS/inventory related regression | Four related suites Passed: `MOBILE_ATTENDANCE_EXPORT_OK`, `OPERATIONS_UX_PERFORMANCE_OK`, `SHIFT_REALTIME_REMINDERS_OK` and `SALES_REPORT_CONSISTENCY_OK`. Existing native photo/export, scoped loading/realtime and report data-consistency contracts remain intact. |
| 2026-07-15 | GPS/inventory production build | `npm.cmd run build` Passed: 695 modules, Vite 13.12 s. The new Inventory page chunk is 51.87 kB (14.18 kB gzip); html2canvas remains a separate on-demand chunk. Existing Browserslist, Tailwind, ExcelJS `eval` and >500 kB warnings remain. No deployment was performed. |
| 2026-07-15 | GPS/inventory final focused check | Both focused regressions Passed again and scoped `git diff --check` found no whitespace error (line-ending notices only). Physical HTTPS GPS accuracy, rendered voucher layout and native Zalo share remain pending because the in-app Browser/device surface is unavailable. |
| 2026-07-15 | Active-deploy comparison | Production index still serves `index-BFBcAEpk.js`/`index-COVblSS-.css`; local build serves `index-MhiE-9x1.js`/`index-CCnBzjj1.css`. Because the production JS hash is unchanged from the earlier bundle inspection, the current GPS, inventory-count image, attendance UX/XLSX, competition, auto-close, sales freshness, Zalo UI, lazy-load and manager-inventory changes remain local-only. Detailed evidence boundary is in `DEPLOY_COMPARISON_2026-07-15.md`. |
| 2026-07-15 | Auto-close schedule clarification | Owner clarifies that day N must close only after entering day N+1: 00:00 UTC+7, not 22:20 of the same day. Source confirms the current endpoint selects `business_date <= today`, which would also risk the just-started day after a cron-only time change. Requirement correction recorded without changing settlement/finalization formulas. |
| 2026-07-15 | Midnight auto-close pre-fix reproduction | `test-auto-close-day.mjs` fails because the previous-business-date helper does not exist; `test-operations-ux-performance.mjs` fails because Vercel still schedules `20 15 * * *`. The corrected contract requires `0 17 * * *` UTC and a UTC+7 previous-day cutoff. |
| 2026-07-15 | Midnight auto-close targeted fix | Changed Vercel cron to `0 17 * * *` UTC and added an explicit previous-UTC+7-business-date cutoff. At 00:00 July 16 the endpoint selects open days through July 15 only, including older stale days for catch-up. `AUTO_CLOSE_DAY_OK`, `OPERATIONS_UX_PERFORMANCE_OK`, app TypeScript and standalone API TypeScript Passed; settlement/session/snapshot/day-close order is unchanged. No deploy or production invocation occurred. |
| 2026-07-15 | Midnight auto-close Vercel build | `npx.cmd vercel build` Passed and emitted `.vercel/output/functions/api/auto-close-day.func`. Readback of `.vercel/output/config.json` confirms exactly `{"path":"/api/auto-close-day","schedule":"0 17 * * *"}`. Scoped diff check has no whitespace error. This is a local preview build only; no deployment or cron invocation occurred. |
| 2026-07-15 | Full-suite inventory and safe regression | Re-read the repository testing workflow/handoff and classified all 17 `scripts/test-*.mjs` checks as local/source/mock-only: none sends a webhook, writes Supabase or mutates production. Syntax Passed 17/17. Functional run Passed 15/17; the only failures are the already-recorded BUG-002 inactive-profile DB guard and BUG-003 spoofable LAN `X-User-*` fallback. Both remain `Blocked — Requires Business Decision`; no permission/authentication contract was changed merely to make tests pass. |
| 2026-07-15 | Cross-module UI button contract | Added `test-ui-button-contract.mjs` and audited 195 native buttons across 20 TSX files. Import-graph reachability identifies 189 buttons in the live `App.tsx` tree and six in unimported legacy `HomePage`/`HistoryPage`; those six cannot appear in the active UI and are not reported as broken controls. Static contract Passed for all: every button has an `onClick` or a form `onSubmit`; no permanent-disabled/no-op handler, submit outside form, form without submit handler, or implicit click+submit collision was found. App TypeScript, all five API files and LAN syntax also Passed. Real click/render/disabled-state verification remains Blocked because the required in-app Browser is unavailable. |
| 2026-07-15 | Stale UI QA harness correction | Source audit found two test-infrastructure mismatches, not product failures: `qa-app-navigation` expected the obsolete label `Lệch kiểm kê` instead of current documented `Kiểm kê`, and `qa-permission-matrix` let `manager-requests` fall through to shift-leader access despite current `App.tsx`/`AppShell.tsx` making it admin-only. Updated only the QA expectations and added a static authorization assertion. Both QA files pass syntax and `AUTH_STATIC_OK (22 routes...)` passes. The separate manager access-policy documentation conflict remains a business-confirmation item; no application permission changed. |
| 2026-07-15 | Core business-control guard reproduction | Added a source-backed guard covering login inactive/branch checks and loading release; own-current-day receipt deletion; positive order quantities and destructive confirmations; kitchen pending→acknowledged→fulfilled buttons; inventory shortage/delete confirmations; two-step/self-protected account deletion; and Control Center delete/purge confirmations. The pre-fix run fails exactly one assertion: `syncBranchToCloud()` throws the obsolete RPC error instead of `directError` when its fallback upsert fails. BUG-031 Medium confirmed; no business rule changed. |
| 2026-07-15 | BUG-031 minimal fix and regression | Changed only the thrown error variable in the branch-sync fallback. `CORE_BUSINESS_BUTTON_GUARDS_OK`, `UI_BUTTON_CONTRACT_OK` (189 reachable/195 total), `ATTENDANCE_NATIVE_CAMERA_LOCATION_OK`, `SHIFT_REALTIME_REMINDERS_OK` and app TypeScript no-emit all Passed. No branch/shift rule, database, external service or production data was changed. |
| 2026-07-15 | Management autosave failure reproduction | Extended the business-control guard after auditing non-button edits that drive management actions. It fails exactly three source-backed assertions: employee payroll, role-default payroll and low-stock threshold autosaves swallow cloud errors while leaving edited local UI values visible. BUG-032 High confirmed because failed payroll configuration can appear saved and other devices can remain stale. No save was sent and no business formula changed. |
| 2026-07-15 | BUG-032 minimal fix and regression | Routed the three rejected autosave promises to existing Admin/Control error banners; debounce timing and saved values are untouched. `CORE_BUSINESS_BUTTON_GUARDS_OK`, `UI_BUTTON_CONTRACT_OK`, `OPERATIONS_UX_PERFORMANCE_OK`, `BUSINESS_COMPETITION_KPI_OK` and app TypeScript Passed. No database or external write was executed by the tests. |
| 2026-07-15 | Cross-module data completeness reproduction | Added `test-data-pagination-consistency.mjs`; pre-fix run fails nine focused assertions. Confirmed BUG-033 (`supply_requests` cut at 80 before report filtering), BUG-034 (cumulative `stock_movements` read lacks stable pagination), and BUG-035 (LAN POS still cuts daily/range reads at 120/1,000 despite the Supabase pagination contract). Official Supabase documentation confirms hosted reads are bounded by default and recommends ordered `range()` pagination. No production query or write was executed. |
| 2026-07-15 | BUG-033/034/035 data-completeness fixes | Added stable 500-row Supabase pagination for cumulative stock movements and authorized supply requests; removed LAN-only caps for supply requests and POS daily/range reads; extended POS regression to cover LAN. `DATA_PAGINATION_CONSISTENCY_OK`, `SALES_REPORT_CONSISTENCY_OK`, LAN syntax and app TypeScript all Passed. No formula, status transition, permission, write path, schema or historical data changed. |
| 2026-07-15 | Configured-SKU inventory reproduction | Extended the inventory regression and reproduced BUG-036 with four focused failures: stock memoization ignores product refresh, the count form limits additions to hardcoded products, render dereferences a hardcoded lookup, and the form receives no product-refresh signal. This can omit or crash on valid admin-configured SKU stock. No product/inventory data or formula changed. |
| 2026-07-15 | BUG-036 configured-SKU inventory fix | Inventory now recalculates stock on the existing product-refresh tick; the count voucher selects and renders from the configured-product source and appends newly loaded positive-stock SKUs without resetting entered counts. `REPORT_INVENTORY_UX_OK`, data-pagination, core business guards and app TypeScript Passed. No stock formula, movement write, schema, role or current count input changed. |
| 2026-07-15 | Business-date pre-fix reproduction | Added `test-business-date-contract.mjs`; it fails eight assertions. Seven application/LAN failures confirm BUG-037: UTC-derived dates can shift inventory/report defaults and same-day schedule/POS guards between 00:00–06:59 UTC+7. The eighth confirms BUG-038 in the Supabase delete RPC; it requires an unauthorized database/authorization migration and is recorded Blocked rather than changed. |
| 2026-07-15 | BUG-037 business-date fix | Replaced UTC-derived client dates with the existing local date helper and LAN guards with an explicit UTC+7 date helper. Business-date regression now fails only the intentionally unresolved BUG-038 Supabase migration assertion. Inventory, core business, POS, LAN syntax and app TypeScript Passed; no status, role, formula, data or database object changed. |
| 2026-07-15 | Critical-read failure reproduction | Added `test-critical-load-failure-visibility.mjs`; pre-fix run fails eight assertions. Seven confirm BUG-039: critical Admin/Handover/Dashboard/POS reads are converted to empty arrays, which can present false zero-sales/no-data state. One confirms BUG-040: Manager Dashboard fetches all shift sessions into state that is never read. No external request or data write occurred. |
| 2026-07-15 | BUG-039/040 critical-read and overfetch fixes | Removed empty-array substitution from Admin requests/reports/POS, Handover attendance/POS and manager POS staff loading; Dashboard receipt failures now show an error while retaining previous data. Removed the unused all-session Dashboard query/state while keeping realtime invalidation. `CRITICAL_LOAD_FAILURE_VISIBILITY_OK`, operations, core-business, POS, realtime and app TypeScript Passed. |
| 2026-07-15 | Final strict regression/build | Full safe suite: 22 total, 19 Passed, three expected failures only (`test-authorization-active-guard`, `test-lan-auth-contract`, `test-business-date-contract`) mapping exactly to BUG-002/003/038. All ten `qa*.mjs` files passed syntax; live Playwright execution was intentionally not run under the Browser-skill constraint. App/API TypeScript, LAN syntax and production build Passed (695 modules, Vite 14.14 s). No deployment, production write, webhook call or database migration occurred. |
| 2026-07-15 | Deploy-polish and leader/KPI batch | Added focused deploy/UI/business tests. Fixed report/archive mobile constraints and month-scoped reads, removed report developer copy and four inventory count cards, made Order image export Blob/native-share safe, hid receipt drilldown from manager, allowed long competition names to wrap, and attributed leader ranking to total revenue inside each leader-owned shift session. `DEPLOY_UI_BUSINESS_POLISH_OK` and related regressions pass; Browser/live-data verification remains pending. |
| 2026-07-15 | KPI reverse audit follow-up | Source evidence showed the detail table did not mirror payroll: it omitted direct POS, split one employee-day by shift id and called the result “theo ca”. Extended `test-kpi-reward-reverse-audit.mjs` failed those three assertions before the fix. BUG-046 is now closed: detail input is allocation-linked plus direct POS grouped by date × branch × employee, labels are per day, and the test plus TypeScript pass. Monetary thresholds were not changed; monthly tiers and six-day perfect-week semantics remain Needs Business Confirmation. |
| 2026-07-15 | Final deploy-package verification | Re-ran 14 focused regressions: 14/14 Passed, including deploy polish, KPI reverse audit, report toolbar/finalization/Zalo, inventory, competition, performance, pagination, critical-read visibility, business guards, 22-route authorization mirror and 195-button contract. The final report pass also replaces node/webhook/Sheet/job-key copy with one operational Zalo retry message. `npm.cmd run build` Passed with 696 modules (Vite 8.54 s); final local assets are `index-BN2tmquW.js` and `index-Dl8Jvmy4.css`. Existing non-fatal Browserslist/Tailwind/ExcelJS/chunk-size warnings remain. No deploy, webhook, production write or migration was performed. |
| 2026-07-15 | BUG-047 idle-button reproduction | Owner reports buttons spinning without any action. New `test-idle-button-loading-state.mjs` fails four focused assertions: CSS adds a rotating pseudo-element and progress cursor to every disabled primary/secondary/mini button. Source fixtures prove several such buttons are idle-disabled only because data/eligibility is absent. This is a confirmed global presentation defect; no API request or business state is involved. |
| 2026-07-15 | BUG-047 focused fix/regression | Removed only the global disabled-button spinner and progress cursor; all disabled conditions, action handlers and real async labels remain unchanged. `IDLE_BUTTON_LOADING_STATE_OK`, `UI_BUTTON_CONTRACT_OK` (195 total/189 reachable), `OPERATIONS_UX_PERFORMANCE_OK` and `CORE_BUSINESS_BUTTON_GUARDS_OK` Passed. Production build remains the next deployment gate. |
| 2026-07-15 | BUG-047 production build and deploy gate | `npm.cmd run build` Passed with 696 modules (Vite 8.35 s); final local assets are `index-MmS9OiBJ.js` and `index-BKCLRZa3.css`. Read-only `vercel env ls` shows only the two public Supabase Vite variables; `SUPABASE_SERVICE_ROLE_KEY` and `CRON_SECRET` are absent locally and on Vercel. Therefore the UI bundle is build-ready, but deploying now would not satisfy the owner-required automatic midnight close. No deploy or environment mutation occurred. |
| 2026-07-15 | Owner-authorized data-safe production prebuild | Owner explicitly authorizes deployment provided active data is unaffected. `npx.cmd vercel build --prod` Passed with 696 modules and emitted `.vercel/output` only. Output inspection contains five expected API functions, static assets `index-DH8SRo4V.js`/`index-BKCLRZa3.css`, and cron `0 17 * * *`; no SQL, migration, seed, purge, LAN store or attendance-selfie file is present. Package scripts contain no pre/post-deploy database action. Missing cron secrets make auto-close reject before creating a service client, so this release cannot mutate data through the schedule. Production deploy is the next authorized step. |
| 2026-07-15 | Data-safe production deployment | Owner-authorized `npx.cmd vercel deploy --prebuilt --prod --yes` succeeded: deployment `dpl_HPtkgYPGrZwA7XD7YjchTQNn2dAq` is `READY` and aliased to `https://gustino-operations.vercel.app`. Read-only verification: index 200 with `Cache-Control: no-store`, exact assets `index-DH8SRo4V.js`/`index-BKCLRZa3.css`, `/api/server-time` 200, and production CSS contains no disabled-button spinner while retaining a static disabled cursor. No migration, SQL, seed, purge, Supabase write, webhook or cron invocation occurred. Automatic 00:00 close remains inactive until its missing server-only secrets are configured. |
| 2026-07-15 | Production today-data synchronization audit | Owner requests current data to run with today's deployment. A new SQL audit is wrapped in `transaction read only`/`rollback`; its first run failed only from an ambiguous audit column and was corrected without product inference. Successful linked production read for 2026-07-15 confirms all seven core tables exist and the three real branches currently total 20 receipts, 22 sold units and 1,210,000đ revenue; each has an open operation day and open shift. A separate active `test` branch also has today's operational rows and is preserved. Because production already uses this same Supabase, copying/seeding would duplicate data; no write has been performed. |
| 2026-07-15 | BUG-048 auto-close snapshot reproduction | Before enabling production cron secrets, source review and a focused test prove the automatic endpoint only preserves prior payload/finalization metadata and does not rebuild completed-day report data. `test-auto-close-day.mjs` fails on an authoritative mocked POS total of 538,000 missing from `summary.revenue` and `dailyReport.totals.revenue`; the archive reads these exact fields. Seven stale live operation days were identified read-only, so cron activation is held to avoid closing them with empty/zero snapshots. No live row, secret or environment value was changed. |
| 2026-07-15 | BUG-048 focused fix | Auto-close now queries authoritative POS receipts for the completed branch/date and merges revenue, sold quantity, employee revenue rows and shift proof images into the archived payload without discarding existing report fields. `AUTO_CLOSE_DAY_OK` and standalone API TypeScript pass. The existing settlement → session close → snapshot → operation-day close order and previous-day cutoff are unchanged; no live data/environment write occurred. |
| 2026-07-15 | Auto-close server-secret configuration | After focused regression and production prebuild passed, the linked Supabase service-role key was retrieved directly through the authenticated CLI and piped to Vercel without printing or writing it to a repository/env file. A cryptographically random `CRON_SECRET` was generated and piped the same way. Vercel confirms both names as Sensitive Production variables. They affect only the next deployment; no endpoint invocation or Supabase row write occurred. |
| 2026-07-15 | Owner-reported post-deploy empty user data investigation | Owner reports all web-user data appears missing and asks to redeploy the most current morning data. Immediate linked read-only audits contradict physical deletion: production still has 36 auth users/36 profiles (32 active), all real-branch profile counts, and today's 20 receipts/22 units/1,210,000đ. A second post-deploy read also shows live operational changes continuing at Lotte Vũng Tàu. The new deployment serves the same frontend asset hashes as the prior release; only the auto-close API changed. Local `data/lan-store.json` was last modified 2026-07-07 and must not overwrite July 15 cloud data. Browser skill setup found no available in-app browser, so direct visual reproduction is pending; no restoration/write is authorized from this report alone. |
| 2026-07-15 | BUG-049 production source-loss reproduction | Bundle/environment inspection found the exact presentation outage. The deployed/local prebuilt JS contains neither the configured Supabase URL nor public anon key. The generated `.vercel/.env.production.local` is stale (2026-06-26) and declares both Vite values empty, overriding the valid local values during `vercel build --prod`. New `test-production-supabase-bundle.mjs` fails against current `dist`. Database and RLS visibility are intact, so no data restore/seed is needed or permitted; emergency rebuild/redeploy is in progress. |
| 2026-07-15 | BUG-049 corrected production package | Re-ran `vercel build --prod` with the two public Vite Supabase variables explicitly loaded from `.env.local` without printing their values. New main asset is `index-MmS9OiBJ.js`; `PRODUCTION_SUPABASE_BUNDLE_OK` passes, and inspected `.vercel/output` contains the correct project host plus BUG-048 receipt-snapshot logic and cron `0 17 * * *`. Auto-close, idle-button, critical-load and operations regressions also pass. No database write/restore/seed occurred; emergency prebuilt redeploy is next. |
| 2026-07-15 | BUG-049 emergency production recovery | Corrected prebuilt deployment `dpl_BwWRkaTpos7Nvf4D7L6gRve5gv2y` is READY and aliased to `https://gustino-operations.vercel.app`. Live index is 200/no-store and serves `index-MmS9OiBJ.js`; names-only/boolean verification confirms the exact configured Supabase host and public anon key are embedded, and the corresponding Supabase Auth settings endpoint returns 200. A final linked read-only audit still shows 20 receipts, 22 sold units and 1,210,000đ across the three real branches; no restore/seed/migration/data write occurred. Users may need to reload/re-login to replace the already-open broken bundle. |
| 2026-07-15 | Permanent BUG-049 build gate | Added `test-production-supabase-bundle.mjs` to the standard `npm run build` command. The guard reads public Vite values from process environment or local env, scans the emitted JS, and fails the build when either the configured Supabase project or public anon key is absent. Full 696-module build now ends with `PRODUCTION_SUPABASE_BUNDLE_OK (index-MmS9OiBJ.js)`. This changes only build verification, not runtime data/business logic. |
| 2026-07-15 | BUG-050 production 404 diagnosis | Owner screenshot shows actions still work but Console logs POST 404 for `active_user_sessions`. Linked read-only runtime-schema audit confirms production lacks `active_user_sessions` and `attendance_adjustment_requests`; all referenced RPCs exist. Source fallback to localStorage preserves button flow but loses cross-device online presence and cloud visibility of attendance adjustment requests. Existing additive migration `20260709_attendance_documents_active_sessions.sql` is the scoped fix, but no production schema change was made without direct approval. |
| 2026-07-15 | BUG-050 owner approval and pre-fix guard | Owner directly authorizes the production migration and prohibits localStorage because it is not synchronized. Added `test-cloud-only-presence-adjustments.mjs`; pre-fix run fails on `activeUsers` device-local persistence. Scope is limited to active-user presence and attendance-adjustment requests; no unrelated business rule/schema is authorized. |
| 2026-07-15 | BUG-050 production migration and cloud-only fix | Applied exactly `20260709_attendance_documents_active_sessions.sql` to linked production. Verification finds both tables, RLS enabled, 3/2 expected policies, and no missing runtime table/function. Without any synthetic row write, the still-open browser produced one real `active_user_sessions` heartbeat, proving POST no longer returns 404. Frontend localStorage/missing-table fallbacks were removed; cloud failures now throw, and Admin surfaces the online-list error while retaining prior state. `CLOUD_ONLY_PRESENCE_ADJUSTMENTS_OK` and TypeScript pass. |
| 2026-07-15 | BUG-050 regression and production package | Cloud-only, attendance camera/GPS, operations performance, critical-load visibility, core guards, 195-button contract and 22-route authorization tests all pass. Vercel production build passes 696 modules and the mandatory Supabase guard; inspected prebuilt asset `index-BfjqIqTV.js` contains the correct Supabase host and neither former localStorage key. Cron remains `0 17 * * *`. Frontend deploy is next. |
| 2026-07-15 | BUG-050 production deployment | Prebuilt deployment `dpl_DW9ghKs3NquGjpZiJ3UjGNeRdCpT` is READY and aliased to `https://gustino-operations.vercel.app`. Live index is 200/no-store and serves `index-BfjqIqTV.js`; direct asset verification confirms the intended Supabase and no active-session/attendance-adjustment localStorage keys. Schema verification still shows both RLS-enabled tables, 3/2 policies and one natural cloud heartbeat. No data seed/delete/overwrite occurred. |
| 2026-07-15 | Production revenue reconciliation | Ran `scripts/db_audit_revenue_reconciliation_20260715.sql` against linked production inside a read-only transaction. The dashboard value 1,332,000đ reconciles exactly between receipt headers and item lines: Gold Coast 538,000đ/7 units, Lotte Vũng Tàu 451,000đ/10 units and Lotte 23/10 343,000đ/7 units. Difference, header/line mismatches, empty/zero receipts, duplicate receipt codes, invalid line prices and allocation mismatches are all zero. No revenue-calculation defect is confirmed; any difference from physical cash/transfer records requires operational receipt-entry reconciliation. All receipts currently carry `payment_method=cash` because the POS source hardcodes that value, so payment-method reconciliation is not reliable and remains a separate business-confirmation item. No production write occurred. |
| 2026-07-16 | BUG-051 manager inventory workspace reproduction | Owner screenshot and source show each branch uses an inline native `details`; expanding a branch with many SKU rows inserts the long detail block before the next branch and pushes the other branches far outside the viewport, creating the reported hidden-branch behavior. New `node scripts/test-manager-inventory-workspace.mjs` fails 15 focused presentation/accessibility/responsive assertions before the fix. Scope is UI only: branch visibility, stock/movement/waste calculations, filters, permissions and database writes are unchanged. Required in-app Browser setup was retried, troubleshooting read and discovery returned `[]`; no alternate browser backend was used. |
| 2026-07-16 | BUG-051 targeted UI fix | Replaced inline branch `details` with a persistent two-column branch-card grid and one React-controlled detail workspace rendered after the complete grid. The detail view presents current SKU stock, existing low-stock threshold/status and a separate waste panel; phone CSS collapses cards/content to one column without horizontal table overflow. `node scripts/test-manager-inventory-workspace.mjs` now reports `MANAGER_INVENTORY_WORKSPACE_OK`. No inventory calculation, filter, permission, API, schema or data write changed; related regression/build remains. |
| 2026-07-16 | BUG-051 regression and build | Six focused suites Passed: manager inventory workspace, report/inventory UX, operations performance, deploy polish, UI-button contract and core business guards. Button audit now finds 197 total/191 reachable; the two added reachable controls are the branch disclosure and detail close actions, both wired. App TypeScript and scoped diff check Passed. `npm.cmd run build` Passed with 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-BCcSFfiz.js)`; existing non-fatal warnings are unchanged. No deploy, database write, webhook or external service mutation occurred. Browser/mobile visual verification remains pending because the required runtime returned `[]`. |
| 2026-07-16 | MOD06/MOD10/MOD12/MOD17 pre-fix reproduction | Added `test-admin-attendance-report-polish.mjs` for the owner-requested 403 repair, Admin attendance correction/list, removal of two redundant KPI cards, sharp fixed-aspect inventory images and professional order presentation. The first run exits 1 because the expected active-session and Admin-correction migrations do not yet exist; source inspection also confirms stock/count exports are variable-size JPEG captures and the current stock report omits a prominent branch. No production call, migration or deployment occurred. |
| 2026-07-16 | BUG-052/053 and owner-requested UI targeted fix | Added least-privilege active-session own-row SELECT migration; added Admin-only attendance correction RPC/client/list with mandatory reason, 18-hour/future validation and shared audit entry; removed the two redundant KPI cards. Stock and count reports now render dedicated 1080×1920 posters and export 2× PNG; order entry is table-like and the order report exports a fixed 1080×1350 PNG. Attendance/payroll continue deriving from authoritative `attendance_records`; no formula or workflow state changed. |
| 2026-07-16 | Focused attendance/report/order regression | `ADMIN_ATTENDANCE_REPORT_POLISH_OK`, app TypeScript, cloud-only presence, mobile attendance/export, report/inventory, manager inventory, operations performance, deploy polish, core business guards and UI button contract all Passed. Button audit finds 200 total/194 reachable and no dead reachable control. Production migration/deploy and Browser/device visual checks remain pending. |
| 2026-07-16 | Attendance/report/order production build | `npm.cmd run build` Passed: Tailwind report CSS, TypeScript project build, Vite 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-DTIpnm5n.js)`. Inventory and Admin chunks are 55.52 kB/100.68 kB; html2canvas remains on demand. Existing stale Browserslist, Tailwind content, ExcelJS eval and large-chunk warnings remain. No migration application, data write or deployment occurred. |
| 2026-07-16 | Extended business regression | Eleven additional checks passed: native attendance/GPS, employee and leader competition, KPI reward reverse audit, shift realtime/reminders, pagination consistency, sales/report consistency, report finalization retry, mobile report toolbar, critical-load visibility, idle-button state and 22-route authorization sync. `test-business-date-contract.mjs` still fails only the already-recorded BUG-038 Supabase POS delete `current_date` rule; it remains `Blocked — Requires Business Decision` and was not changed to make the test pass. |
| 2026-07-16 | Owner correction — flexible inventory-count report | Owner rejects the fixed 1080×1920 poster because five rows leave a long empty page and supplies the compact paper-style inventory form as the required visual reference. Updated focused test fails before the fix on dynamic `scrollHeight`, paper snapshot/header and removal of fixed 1920px height. This supersedes only the count-image presentation; inventory values, count inputs, signatures and persistence remain unchanged. |
| 2026-07-16 | BUG-053 flexible report fix | Count export now measures the rendered poster `scrollHeight` and captures only that height. The poster follows the supplied reference: white paper, slim green top rule, centered bilingual title, one system-stock strip, bordered metadata grid with branch/reporter, dark compact table and signatures. Focused report test, inventory regression and TypeScript pass; no inventory value or persistence changed. |
| 2026-07-16 | Flexible-report deployment gate | Eight related suites and the normal production build pass; local asset is `index-CrBhvNaD.js` with `PRODUCTION_SUPABASE_BUNDLE_OK`. First `vercel build --prod` is correctly rejected by the mandatory guard because the isolated Vercel build did not inject the public Supabase values, despite its env file containing non-empty names. No deploy occurred. Rebuild must explicitly load only the two public `VITE_` values, as required by `TESTING_CONFIG.md`. |
| 2026-07-16 | Corrected Vercel prebuild | Re-ran the cached Vercel CLI with only the two public Supabase build values loaded from ignored `.env.local`. Prebuild Passed with `PRODUCTION_SUPABASE_BUNDLE_OK (index-CrBhvNaD.js)`. Inspection finds the expected API functions/cron/static assets and `NO_DATA_OR_MIGRATION_ARTIFACTS`; no seed/purge/SQL file is in the deploy package. |
| 2026-07-16 | Migration safety and execution blocker | Static audit reports `MIGRATION_NO_DESTRUCTIVE_DATA_DDL_OK`: no delete-from, truncate, drop-table or drop-column exists. The only `attendance_records` update is inside the Admin RPC and does not execute when the migration is installed. Supabase CLI acquisition/execution is blocked by the environment usage limit, which explicitly prohibits an indirect workaround. Therefore neither migration nor deployment was executed; releasing frontend first would leave the 403/RPC mismatch and is intentionally withheld. |
| 2026-07-16 | Targeted production migration application | The Supabase CLI became available and linked migration history was inspected first. Because local/remote histories are intentionally not aligned, `db push` was rejected. Applied only `20260716_active_session_own_read.sql` and `20260716_admin_attendance_correction.sql` with `db query --linked --file`; both exited 0. A follow-up catalog assertion exited 0 and proves the owner/Admin SELECT policy exists, the obsolete Admin-only policy is gone, the Admin correction function is `security definer`, and `authenticated` has execute permission. The migration files contain no executed attendance/POS/inventory DML; the attendance update remains inside the RPC body and was not invoked. Frontend deployment remains pending. |
| 2026-07-16 | Data-safe production deployment and verification | Deployed only the inspected `.vercel/output` as production deployment `dpl_Cnc2dpznvj7TqPUGWXR7cagXKmBn`; Vercel reports `READY` and aliased `https://gustino-operations.vercel.app`. Live readback returns 200 for `index-CrBhvNaD.js` and `index-NumTK3Ca.css`, confirms the intended Supabase host, Admin attendance RPC reference and inventory poster CSS. Unauthenticated `/api/auto-close-day` returns 401 before database access. A post-deploy linked SQL assertion confirms profiles remain at least 36, attendance and stock history remain present, and 2026-07-16 sales remain at least the previously verified 22 receipts/1,332,000đ. No restore, seed, purge, manual cron call, attendance correction or synthetic business write was executed. Signed-in heartbeat and physical mobile/image visual checks remain pending. |
| 2026-07-16 | BUG-054 attendance-correction list scalability discovery | Owner reports the Admin correction list becomes unmanageably long across multi-month ranges. Source confirms the UI renders `attendanceListRows.map(...)` with no exact-day selector, no employee selector and no pagination; `employeeId` state exists but no control renders it. Directly authorized UX scope: default to one exact day, allow one employee across the current report range, and paginate at 20 rows without changing the authoritative correction RPC, payroll sync, export range or attendance formulas. Browser skill setup was retried, troubleshooting read and discovery returned `[]`; no alternate browser backend is used. |
| 2026-07-16 | BUG-054 automated pre-fix reproduction | Extended `test-admin-attendance-report-polish.mjs` with exact-day/one-employee view, 20-row pagination, dedicated filter/pagination styling and prohibition of direct full-list rendering. The pre-fix command exits 1 at the first missing page-size assertion, matching the source reproduction. No API, database, attendance record or production service was touched. |
| 2026-07-16 | BUG-054 targeted UI fix | Admin correction now defaults to `Theo ngày`, clamps the selected day inside the global report range, and shows all matching employees for that date. `Theo nhân viên` requires one employee and shows only that person's shifts within the unchanged global range. Both modes sort newest first and paginate at 20 rows; filter/page changes close any stale inline editor. The focused regression now reports `ADMIN_ATTENDANCE_REPORT_POLISH_OK`. Export range, correction RPC/audit/validation, realtime refresh and payroll calculations are unchanged. Related regression/build remains pending. |
| 2026-07-16 | BUG-054 related regression | Eight commands Passed: focused attendance correction, mobile attendance/export, cloud-only presence, operations performance, core business guards, KPI/payroll reverse audit, app TypeScript and UI-button contract. Button audit now finds 204 total/198 reachable; the four new reachable controls are two list modes and two pagination buttons, all wired with explicit `type=button`. No attendance/payroll formula, RPC, database or production data was changed. Production build and visual Browser check remain. |
| 2026-07-16 | BUG-054 production build | Final focused regression and TypeScript recheck Passed after preventing an empty exact-date value. The final `npm.cmd run build` Passed with 696 modules and mandatory `PRODUCTION_SUPABASE_BUNDLE_OK (index-72SMd5zB.js)`; CSS is `index-CLMsA5QW.css`. Existing Browserslist, Tailwind-content, ExcelJS-eval and >500 kB warnings remain non-fatal. No migration, production data write or deployment occurred. Visual Browser/mobile confirmation remains pending because the required in-app Browser is unavailable. |
| 2026-07-16 | BUG-055 manager inventory quantity/export discovery | Owner requests an exact-day table of goods issued for sale, explicit kg/g/item units, clearer/larger text, removal of the warning-threshold column and complete Excel export. Source confirms branch cards sum `expected`, inbound and outbound across incompatible units into unlabeled numbers (for example kg plus items); the existing Excel has only one period-summary sheet; and the SKU table still renders the rejected threshold column. `sale_out` is the established source-backed movement for “hàng lấy ra bán”, distinct from processing/packing/waste. Browser connection remains unavailable after required skill troubleshooting; no alternate backend is used. |
| 2026-07-16 | BUG-055 automated pre-fix reproduction | Extended `test-manager-inventory-workspace.mjs` and ran it before implementation. It exits 1 with 13 focused failures: rejected threshold column, missing exact-day sale-out filter/table, mixed-unit/unlabeled card totals, five missing workbook sheets and missing responsive daily-outbound CSS. The test is source-only; no inventory API, workbook download, database or production data was touched. |
| 2026-07-16 | BUG-055 targeted inventory UI/export fix | Manager inventory now groups every displayed total by product unit instead of adding kg and items together; thousands use spaces, decimals use a comma, and sub-kilogram values display as grams. Added an exact-date `sale_out` table grouped by branch/SKU with quantity, unit, voucher count, last time, recorder and notes. Removed only the threshold column, retained derived stock status and increased desktop/mobile typography. Inventory Excel now has five sheets: period summary, selected-day sale-out, complete period ledger, current stock and count vouchers. Focused regression now reports `MANAGER_INVENTORY_WORKSPACE_OK`. First TypeScript run caught a wrong module import for configured-product helpers; the import was corrected and TypeScript now exits 0. No movement formula/type/write or production data changed. |
| 2026-07-16 | BUG-055 related regression | Nine commands Passed: manager inventory workspace, report/inventory UX, data pagination, operations performance, deploy polish, core business guards, 204-button contract, prior Admin attendance/report polish and app TypeScript. Configured products now feed the manager summary/export through the existing product cache, while stock movement signs/types and reads remain unchanged. No browser download, database write or production mutation occurred. Scoped diff/build remain. |
| 2026-07-16 | BUG-056 daily competition/realtime discovery | Owner requires a daily competition table as a classification, not another card, and requests a final realtime/data-sync audit before deployment. Source confirms the current daily view is a fixed “Top 3 today” `CompetitionMiniBoard`; it cannot select another day. Management inventory/competition subscribes to relevant Supabase tables, but unlike attendance it has no focus/visibility refresh or periodic fallback after an interrupted realtime channel. New focused test exits 1 at the missing classification state before implementation. Browser skill setup/troubleshooting again finds no in-app Browser (`[]`), so no alternate UI backend is used. No database or production write occurred. |
| 2026-07-16 | BUG-056 targeted fix | Replaced the two competition mini-cards with one shared ranking table classified as employee/day, employee/month or shift leader/month. Daily mode selects an exact date inside the active range and shows up to ten ranked rows instead of a fixed Top 3 today. Management keeps its existing table-specific Supabase subscriptions and now also reconciles silently every 30 seconds plus on focus/visibility, using the existing coalesced refresh guard. Focused test now reports `MANAGEMENT_DAILY_COMPETITION_REALTIME_OK`; inventory, Admin attendance and app TypeScript regressions also pass. Revenue/KPI/payroll/stock formulas, writes and schema are unchanged. |
| 2026-07-16 | BUG-056 legacy-test conflict resolution | Broad regression exposed two stale assertions in `test-business-competition-kpi.mjs` requiring the old daily Top-3 and leader mini-cards. This conflicts with the owner's direct replacement requirement, so only those presentation assertions were updated to require the shared day/month/leader classification; all revenue-positive filters and stored KPI formula assertions remain unchanged. The corrected test reports `BUSINESS_COMPETITION_KPI_OK`. |
| 2026-07-16 | Predeploy read-only audit attempt 1 | The new linked SQL audit was rejected before returning data because an unqualified `business_date` was ambiguous between the target CTE and receipt table. No statement ran past PostgreSQL parsing and no product bug/data mutation is inferred. Qualified every audit column and will rerun the same read-only transaction. |
| 2026-07-16 | Predeploy linked data/realtime audit | Corrected read-only transaction Passed for business date 2026-07-16. Baseline counts: 36 profiles, 246 attendance records, 351 registrations, 739 receipt headers/855 lines, 686 stock movements, 3 inventory reports and 45 shift sessions. All eight required realtime tables are published. The Admin attendance RPC and active-session owner/Admin policy exist. Today's branch receipts total 27 and 1,270,000đ; nine attendance records remain open at audit time. Today's warehouse has 22 movements but zero `sale_out` lines because no sale-out/closed-shift posting exists yet; the new table must therefore truthfully show no sale-out rows until operations records them. No database write occurred. |
| 2026-07-16 | Postdeploy API-check harness correction | Vercel inspection already proves the new deployment is Ready and exposes the expected five functions. The first unauthenticated cron probe used PowerShell's unavailable `SkipHttpErrorCheck` parameter and therefore produced status 0 without making a valid assertion; no product failure is inferred. Rerun uses compatible exception-status capture. |
| 2026-07-16 | BUG-055/056 regression and production build | Twelve related scripts Passed: daily competition/realtime, manager inventory, business KPI, KPI reverse audit, operations performance, shift realtime, cloud-only presence, pagination, business button guards, 204-button contract, report/inventory and Admin attendance. Scoped diff check has no whitespace error. Normal and Vercel production builds both Passed with 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-C9vho61t.js)`; CSS is `index-CCcaMT4s.css`. |
| 2026-07-16 | Data-safe warehouse/competition deployment | Inspected prebuilt output contains 61 files, five expected API functions/cron and no SQL, migration, seed, purge or data path. Deployed production `dpl_9zfvjMPrFbzzuFrzF5P3mnk4moDM`; Vercel reports Ready and aliases `https://gustino-operations.vercel.app`. Live index/assets return 200 and contain exact-day warehouse sale-out, classified competition, silent realtime fallback and responsive CSS. Unauthenticated auto-close returns 401. Postdeploy read-only counts did not decrease: profiles 36, attendance 246, registrations 351, stock 686, reports 3 and sessions 45 stayed equal; receipts increased naturally during deployment from 739/855 lines to 741/857. No migration was needed, and no seed/purge/manual cron/business write was run. Browser/device and actual workbook-open verification remain unavailable/pending. |
| 2026-07-16 | POS versus `sale_out` production diagnosis | Owner asks why morning sales do not appear as warehouse sale-out. Read-only production audit proves 30 receipts/30 item lines, 31 products and 1,458,000đ today, but every receipt line has `allocation_id = null`; all three branch sessions are open, there are no today's bag allocations and no `sale_out`. Source confirms `saveSalesReceipt` writes only receipt header/items, while `sale_out` is created by the warehouse “Xuất bán” voucher or shift close from bag allocations. Therefore this is not a display/realtime failure: current POS and warehouse-issue flows are not automatically linked. Whether a POS sale should automatically deduct inventory is `Needs Business Confirmation` because “sold to customer” and “issued from warehouse to sell” are different events. No data was changed or shift closed. |
| 2026-07-16 | Owner clarification — remaining goods are the handover balance | Owner confirms no separate “return leftovers” function belongs in the workflow; the existing closing/handover count is the remaining stock inherited by the next shift/day. For a completed shift, consumed/out quantity must therefore be derived from opening balance + finished-goods additions during the shift − closing handover balance − separately recorded waste. POS quantity is the reconciliation value, not a second stock deduction. While a shift is open, this derived value is provisional because the closing balance does not exist yet. No implementation or data change was made in this diagnostic turn. |
| 2026-07-16 | BUG-057 handover-based Out implementation intake | Owner authorizes the clarified calculation locally and explicitly prohibits deployment. Production/source evidence shows handover opening/closing balances contain the four bulk finished-product SKUs, while POS products have source conversion factors through packing options. New focused test fails before implementation because the manager inventory route neither loads/realtime-subscribes to sessions/POS nor renders the required shift reconciliation/Excel sheet. Scope is read-only derivation and presentation: no stock movement, shift state, receipt, schema or historical data write. |
| 2026-07-16 | BUG-057 targeted local implementation | Manager inventory now loads/realtime-subscribes to shift sessions and POS in inventory scope, converts POS packages to their configured source quantities, and renders one responsive per-shift reconciliation. Open shifts show POS provisional and no official Out; closed shifts calculate `opening + additions - closing - waste` and compare it with POS. Each shift expands to SKU details, branch cards show live POS, and Excel adds `Đối chiếu ca` while retaining raw sale-out vouchers. Focused test reports `INVENTORY_SHIFT_RECONCILIATION_OK` and app TypeScript exits 0. No movement, receipt, session, schema, production data or deployment changed. |
| 2026-07-16 | BUG-057 inventory contract regression | Updated the prior manager-inventory presentation test from the superseded raw sale-out table requirement to the owner-approved handover/POS reconciliation, while retaining exact `sale_out` export checks and adding the sixth Excel sheet requirement. Four related commands Pass: `MANAGER_INVENTORY_WORKSPACE_OK`, `INVENTORY_SHIFT_RECONCILIATION_OK`, `REPORT_INVENTORY_UX_OK` and `OPERATIONS_UX_PERFORMANCE_OK`. |
| 2026-07-16 | BUG-057 broad regression/build/runtime | Ten related regression commands Passed, including pagination, button/business guards, KPI and shift realtime; UI contract remains 204 total/198 reachable. Scoped diff check has no whitespace error. Final local build Passed with 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-CcbqSOlp.js)`; CSS is `index-CMcoy5E8.css`. Local web/API are HTTP 200 and the served Admin module contains the reconciliation class/formula/session data. No Vercel command, migration, production query write or deployment was performed. |
| 2026-07-16 | BUG-058 migration/schema gate | Linked migration list and `db push --dry-run` prove the local/remote version names are intentionally unaligned; bulk push is rejected and was not repaired or forced. Read-only audits find all 30 required tables and 17 required functions present, all eight realtime tables published, required RLS/policies/RPC installed, and `products.deleted_at` live with 9 tombstones. BUG-057/058 add no migration, so the safe required migration count is zero. Replaying every local SQL file is prohibited because several historical migrations contain purge/backfill/cleanup DML. |
| 2026-07-16 | BUG-057/058 production prebuild | Eight focused regressions Passed: product tombstone, shift reconciliation, manager inventory, daily competition/realtime, Admin attendance list, cloud-only presence, POS/report consistency and operations performance. Vercel prebuild passed with 696 modules and `PRODUCTION_SUPABASE_BUNDLE_OK (index-DOyCiHFj.js)`. Inspection found 61 files, five API functions, cron `0 17 * * *`, the correct Supabase public config and no SQL, migration, seed, purge, LAN store or local data artifact. |
| 2026-07-16 | BUG-057/058 data-safe production deploy | Prebuilt deployment `dpl_GEMYw5wPiK7fTSK5j6wjG9WBbgAX` is Ready and aliased to `https://gustino-operations.vercel.app`. Live index/assets return 200/no-store, main JS contains the tombstone contract, Admin chunk contains `Đối chiếu ca`, server-time returns 200 and unauthenticated auto-close returns 401 before database access. Runtime schema remains 30/30 tables and 17/17 functions. |
| 2026-07-16 | Postdeploy data reconciliation | Profiles 36, attendance 250, registrations 351, stock movements 702, inventory reports 3 and shift sessions 45 are unchanged from the immediate predeploy baseline. Product rows/tombstones remain 34/9. Receipt headers/items changed from 755/873 to 754/872, exactly one 33,000đ receipt at Lotte Vũng Tàu. No migration was applied and the inspected deploy artifact has no database write path; the application exposes only an explicit confirmed `delete_pos_receipt` action and database statistics show that RPC has been called historically, but it does not log actor/time. Therefore the one concurrent POS deletion is disclosed and not silently classified as deployment data loss; no speculative restore was attempted. |

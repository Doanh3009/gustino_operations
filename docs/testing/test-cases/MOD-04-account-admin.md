# MOD-04 — Employee/account administration

Status: in progress; isolated LAN account lifecycle/browser integration passed, production Edge deployment and exact affected-username verification pending.

| ID | Flow | Expected result | Automation/status |
|---|---|---|---|
| MOD04-TC-CRM-01 | Open Admin employee/branch directories and select one record | Overviews are compact lists; branch opens a restaurant profile; employee opens a profile containing avatar/work/role/password/delete controls, while list rows contain none | `ADMIN_CRM_DIRECTORY_OK`; TypeScript/build Passed; visual verification pending |
| MOD04-TC-CRM-02 | Review directory without creating an account, then open/close create panel and enter/leave detail | Create form is hidden behind `+ Thêm nhân viên`; branch/employee details have explicit sticky back actions | `ADMIN_CRM_DIRECTORY_OK`; visual verification pending |
| MOD04-TC-CRM-03 | Add or inspect a branch from Admin CRM | `+ Thêm chi nhánh` is beside the branch directory; Control Center has no branch-creation form; detail shows branch address/manager | `ADMIN_CRM_DIRECTORY_OK` Passed |
| MOD04-TC-CRM-04 | Edit an employee lifecycle | Admin can persist probation/working/ended plus valid dates/notes; ended requires an end date; account activity/payroll rules are unchanged | Migration applied; linked read-only catalog/RPC verification and `ADMIN_CRM_DIRECTORY_OK` Passed |
| MOD04-TC-CRM-05 | Inspect payroll and actual sales inside employee detail | Up to 36 saved payroll rows are shown; hourly records do not fabricate received totals; POS receipts use existing seller-linked receipt data for the selected range | `ADMIN_CRM_DIRECTORY_OK` + TypeScript Passed |
| MOD04-TC-CRM-06 | Open employee detail while Admin is focused on Accounts | Accounts loads and realtime-subscribes to receipts/items/attendance; seller ID is authoritative and legacy name fallback is restricted to the employee branch | `ADMIN_CRM_DIRECTORY_OK` + TypeScript Passed |
| MOD04-TC-CRM-07 | Search/filter employee directory and switch employee functions | Directory filters by text, branch and employment status; profile separates Overview, Attendance, Sales, Payroll and Account without exposing actions in list rows | `ADMIN_CRM_DIRECTORY_OK`, UI-button contract, TypeScript and build Passed |
| MOD04-TC-CRM-08 | Review receipts that cannot be assigned to one employee | Unknown seller IDs and legacy names that do not resolve uniquely inside the receipt branch remain visible in `Doanh số chưa phân bổ` | `ADMIN_CRM_DIRECTORY_OK` Passed |
| MOD04-TC-CRM-09 | Change Accounts branch/date range and refine employee directory | Branch and employee figures refresh for arbitrary from/to dates; directory combines text, branch, employment status, role and work-group filters | `ADMIN_CRM_DIRECTORY_OK` + TypeScript/build Passed |
| MOD04-TC-CRM-10 | View attendance correction around 721–1180px content width | Edit/delete actions move to a dedicated row and remain inside the card instead of overflowing right | Source/CSS regression Passed; live Browser unavailable |
| MOD04-TC-CRM-11 | Open Admin Overview and select an operational alert | Revenue, probation, missing checkout, pending-order and low-stock cards show period-scoped counts and navigate to the existing module | `ADMIN_CRM_DIRECTORY_OK`, UI-button contract and TypeScript/build Passed |
| MOD04-TC-CRM-12 | Open a branch and switch its functional tabs | Overview, Revenue, Employees, Attendance, Inventory and Orders render branch/date-scoped existing data without changing formulas or workflows | `ADMIN_CRM_DIRECTORY_OK` + TypeScript/build Passed |
| MOD04-TC-POS-01 | Admin creates a branch-assigned cashier | Role is accepted through frontend/Edge/LAN, requires an active branch and defaults to POS-only navigation | `CASHIER_POS_WORKSPACE_OK`; Edge deploy pending |
| MOD04-TC-POS-02 | Cashier checks out a cash/QR/card sale | Server validates branch/date/lines/payment, atomically creates header+items, calculates cash change and returns a lock-safe receipt code | Migration applied; catalog audit + `CASHIER_POS_WORKSPACE_OK` Passed |
| MOD04-TC-POS-03 | Open a completed receipt and print | Receipt includes branch, payment method, customer paid/change and uses a print-scoped 80mm layout without affecting other report print CSS | Source/CSS regression Passed; physical printer verification pending |
| MOD04-TC-CRM-13 | Navigate between unified Admin sections | Only Overview requests all management sources; each other section fetches/subscribes only to its declared datasets, preventing the prior all-module load on every click | `CASHIER_POS_WORKSPACE_OK`, TypeScript/build Passed |
| MOD04-TC-CRM-14 | Use Admin/POS on desktop and phone | Shared visual tokens provide compact navigation/cards/forms/tabs; phone layouts use sticky filters, two-column metrics/menu and single-column checkout | Source/CSS regression Passed; live Browser unavailable |
| MOD04-TC-CRM-15 | Navigate Admin module/profile/subpage, refresh, then use Back/Forward | URL identifies the active section and detail page; refresh preserves it; browser history restores the prior page without a full application reload | `ADMIN_CRM_DIRECTORY_OK`, `CASHIER_POS_WORKSPACE_OK`, lazy-route regression and build Passed |
| MOD04-TC-CRM-16 | Paginate filtered employee records | Pagination renders only the selected record slice, reports total/range, supports 10/20/50 rows and numeric previous/next navigation without changing the Admin module route | `ADMIN_CRM_DIRECTORY_OK` + TypeScript/build Passed |
| MOD04-TC-CRM-17 | Open employee and branch module URLs | `#/admin/employees` renders only the independent employee table page and its record pagination; `#/admin/branches` renders only the branch directory; module navigation never uses pagination | `ADMIN_CRM_DIRECTORY_OK`, `ACCOUNT_ORPHAN_RECOVERY_OK`, TypeScript and build Passed |
| MOD04-TC-CRM-18 | Search and paginate branches | Branch search filters list records; pagination slices only branch rows and keeps the branch module route; each row opens the durable branch profile URL with connected staffing, receipt count and period revenue | `ADMIN_CRM_DIRECTORY_OK`, TypeScript and build Passed |
| MOD04-TC-CRM-19 | Navigate from Admin sidebar or Dashboard command cards | Every module action resolves through the central Admin route map; browser URL/history identify the destination and no action only mutates section state; module pages do not render the former “Đang xem” hero/demo card | CRM/cashier/lazy-route regressions, TypeScript and build Passed |
| MOD04-TC-CRM-20 | Open the Admin Overview route | Independent `DashboardPage` renders only connected summary metrics, operational alerts, recent activity/waste and online sessions; it does not embed employee, attendance, payroll or inventory module lists | `ADMIN_CRM_DIRECTORY_OK`, cashier/account regressions, TypeScript and build Passed |
| MOD04-TC-CRM-21 | Navigate Admin on desktop, tablet and small mobile | Desktop/tablet retain the fixed 252px left sidebar (72px collapsed), small mobile opens the same menu as a hamburger drawer, active state follows URL-derived page/section, and no horizontal bottom navigation or dock spacing remains | `FIXED_SIDEBAR_NAVIGATION_OK`, CRM/cashier/lazy-route regressions, TypeScript and build Passed |
| MOD04-TC-SYNC-01 | Load account administration with an inactive profile that still has a login email | The account remains visible as `Đã xóa`, while operational employee reports continue to exclude it | `ACCOUNT_ORPHAN_RECOVERY_OK` + TypeScript Passed |
| MOD04-TC-SYNC-02 | Permanent Auth deletion fails | The profile is not removed after the Auth failure, so the occupied username does not disappear from account administration | `ACCOUNT_ORPHAN_RECOVERY_OK` Passed; Edge deploy pending |
| MOD04-TC-SYNC-03 | Create an exact-email username whose Auth user exists but profile is missing | Recover only after proving the profile is absent; otherwise report the existing account name/status | `ACCOUNT_ORPHAN_RECOVERY_OK` Passed; production audit currently has zero true orphans |
| MOD04-TC-DELETE-02 | Hard-delete an inactive test account | Inactive account remains visible with username-retention warning; Admin can invoke hard-delete, Auth is removed before profile, and signed-in account remains protected | `ACCOUNT_ORPHAN_RECOVERY_OK`; production has four legacy inactive Auth/profile pairs, no automatic deletion performed |
| MOD04-TC-LAN-01 | Admin creates a local QA staff account, employee signs in, Admin resets the password, then deactivates the account | New password works, old password stops working, deactivated account cannot sign in, and the isolated profile retains `active=false` with its password hash cleared | `SHARED_SCHEDULE_ACCOUNTS_QA_OK` against a fresh isolated LAN store; final store has exactly one inactive QA account and no API error log |
| MOD04-TC-LAN-02 | Open staff weekly registration and Admin employee directory in the same isolated runtime | Staff sees the current vertical weekly board; Admin opens `#/admin/employees`, expands `Tạo mới`, and sees the account form/safety note | `SHARED_SCHEDULE_ACCOUNTS_QA_OK`; browser integration Passed |

Production read-only audit on 2026-07-18 found 37 Auth users and 37 profiles, with zero missing rows in either direction. No account was deleted, recreated or reset during the audit.

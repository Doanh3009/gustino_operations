# Decision Log

## 2026-07-21 — incident fixes stay inside existing business contracts

- Keep the owner-confirmed permanent `Xóa nhân viên` behavior. Fix only the missing route/state recovery so a removed ID cannot blank the personnel workspace; do not reintroduce deactivation or a second test-delete action.
- Keep both approved reverse-geocode providers concurrent. Give the street/ward source only an 800 ms bounded preference after the coarse administrative source resolves, then fall back; do not weaken fresh GPS, selfie, ≤150 m or concrete-address gates.
- Treat revenue snapshots as point-in-time baselines and add only receipts created later. Add reconciliation timers/focus/visibility to recover from dropped realtime subscriptions; do not change revenue formulas, prices, receipt ownership or report history.
- Fix Orders phone overflow only in CSS layout/word wrapping. Do not change request data, status transitions, permissions or export dimensions.
- Do not invoke a synthetic production report/n8n workflow or delete an employee to prove this batch. Those actions mutate business/external state and require the real authorized operator; local/static/integration evidence is sufficient for the implementation gate.

## 2026-07-20 — test MOD-04 account mutation only in an isolated LAN store

- Use the seeded loopback Admin and a fresh `GUSTINO_DATA_DIR` for create/login/reset/deactivate integration. Never exercise those mutations against production Auth merely to obtain test coverage.
- Follow the durable Admin employee route and collapsed create-panel contract already enforced by current source/regressions. Update stale UI selectors without changing product presentation or account behavior.
- LAN success is evidence for the local implementation only; it does not close the pending production Edge/Auth verification.

## 2026-07-20 — keep MOD-08 integration data isolated and align the harness to current rules

- Run the mutating handover browser flow only against a fresh `GUSTINO_DATA_DIR` created for QA; do not point it at the repository's normal LAN store or Supabase.
- Treat self-registration, automatic shift opening after leader check-in, and direct close-to-report navigation as the current source-backed contracts. Update the stale harness rather than reintroducing removed controls or weakening attendance ownership.
- Do not classify a harness expectation mismatch as a product bug. The corrected run must prove persisted end state as well as browser selectors before recording integration Passed.

## 2026-07-20 — correct stock-unit presentation without changing the authorized issue rule

- Superseding correction from the owner: manual warehouse sale-out is not hard-blocked when availability is insufficient. Keep the existing explicit confirmation and direct exception after confirmation. Fix the actual kg/g default and misleading `Đủ bán` presentation; do not round away physical residue or change movement arithmetic.
- Decision: derive “not enough to pack” only from the existing `PACKING_OPTIONS_BY_OUTPUT` source quantities. This is a presentation warning, not a new movement/stock restriction.
- Decision: classify active priced non-kg finished SKUs as “Món trong menu bán” only for presentation. Product category, unit, ID, price, movement signs and stock quantity remain unchanged.
- Decision: Admin purchasing is observation/export only. Operational users and kitchen retain their existing creation/status workflows; Admin no longer mutates request state from its report workspace.
- Decision: do not create/apply a database migration without direct owner approval. Source proves migration-order drift in `create_stock_movements_checked`, but repository safety rules explicitly reserve schema changes for an approved operation.

## 2026-07-20 — attendance reliability and competition layout remain implementation-only fixes

- Race the two already-approved reverse-geocode providers because the old sequential 10-second maximum exceeded the phone client's 4-second timeout. Keep fresh GPS, ≤150 m, concrete address and selfie requirements unchanged.
- Treat the competition table issue as CSS clipping: allow overflow and use labelled cards at tablet width. Do not change ranking aggregation, KPI classification or rewards.

- 2026-07-18 — Fix the reported phone overflow only at the presentation boundary. The on-screen Bàn giao actions and report poster may stack at narrow widths, while the 1080px export poster keeps its exact dimensions and is moved fully off-canvas with a viewport-independent coordinate. Do not hide actions, shrink business data, or change report/n8n/finalization behavior.

- 2026-07-18 — Reuse the existing current-day `ReportPage` for the shift leader's post-handover review. Add only a visible handover action after a same-day shift is closed and keep its existing connected export labelled `Lưu ảnh`. Do not create another report/archive surface and do not retrigger or alter n8n automatic delivery when the operator merely views or saves the poster.

- 2026-07-18 — Restore Manager read access to the existing daily report archive and decouple sales attribution from attendance registration. The owner's direct requirement that Manager retain the old design does not remove the archive permission already documented and enforced by RLS. A valid POS receipt remains authoritative evidence of its seller/revenue even when scheduling data is missing; attendance fields may remain absent, but the seller row must not be discarded. Keep the existing register-shift-before-check-in workflow and route an unscheduled employee to that flow instead of fabricating a shift or attendance record.

- 2026-07-18 — Router changes modules/details; pagination changes records inside one module only. The owner supplied the complete Admin redesign contract and explicitly rejected fake tabs/section visibility as page navigation. Admin uses domain routes, while reusable pagination owns only page/page-size slicing. Remaining module extraction must preserve API/business logic and cannot be claimed complete while `AdminPage.tsx` still contains legacy section implementations.

- 2026-07-18 — Add `cashier` as a branch-scoped POS-only role and consolidate Admin navigation into one Management workspace. The owner directly authorized a TP.HCM cash-register permission, full receipt/payment/printing support, database changes and removal of old Admin presentation. Cashiers default to Sales, cannot access operational/Admin modules, and server checkout forces their own seller identity and assigned branch. Existing staff/shift-leader workflows remain unchanged.

- 2026-07-18 — Store employment lifecycle as administrative metadata without changing account activity, payroll formulas or permissions. The owner directly authorized the CRM database connection/status tracking. `probation`, `working` and `ended` plus their dates/notes are persisted through an Admin-guarded RPC; they do not toggle `profiles.active`, Auth access, attendance or compensation calculations. Saved hourly payroll rows show the hourly rate rather than inventing a “received” total without worked-hours/payment evidence.

## 2026-07-18 — Admin Accounts uses CRM master-detail presentation

- Direct owner requirement supersedes the prior card-based CRM preview: branches and employees are compact lists, and employee actions appear only after opening that employee's profile.
- Existing handlers, permission guards and data calculations are retained; only presentation and action placement change.
- The create-account form remains on the directory view because it is not an action belonging to an existing employee.
- Branch detail is read-only and uses already-loaded branch, POS and employee data for the selected period.
- No deploy, account mutation, schema or business-rule change is authorized by this batch.

## 2026-07-18 — Preserve the last valid shared product catalog across reloads

- Decision: cache the last successful merged cloud product catalog and tombstones locally, using it only when the current cloud read fails.
- Reason: a custom company-wide SKU cannot be reconstructed from the built-in catalog; losing the in-memory cache on reload made `TP-BANH` disappear despite the zero-stock visibility fix.
- Invariants: Supabase remains authoritative; successful reads replace the fallback; no stock quantities, SKU records, formulas, or delete state are invented or written by the fallback.

## 2026-07-18 — Make handover the sole shift-finalization surface

- Decision: treat Report as the automatic completion view entered only by a successful handover intent; remove its normal operations navigation and all manual Zalo/share actions.
- Ca 1 invariant: save only its shift snapshot and send only the Ca 1 poster.
- Ca 2 invariant: require no open shift, finalize the existing daily snapshot atomically, then send Ca 2 and Total-day posters.
- Reliability: keep the handover intent until n8n confirms; a reload retries only delivery when the report snapshot was already saved, avoiding duplicate finalization.
- Admin CRM remains a read-only presentation over existing personnel, POS, attendance and branch scope; it adds no revenue formula, API contract or schema.

| Date | Decision | Evidence and consequence |
|---|---|---|
| 2026-07-18 | Apply only `20260717_supply_request_delivery_schedule.sql` as targeted linked SQL; do not reconcile or bulk-push migration history in this release. | Pre-apply runtime audit proved every latest required object except the two nullable delivery columns and their check constraint was already installed. The migration contains additive nullable DDL only; post-apply audit proves the exact objects exist and predeploy data/realtime baselines are intact. |
| 2026-07-18 | Do not restore the one account or bag session that disappeared during the deploy window. | Postdeploy account audit proves Auth and profiles both changed from 37 to 36 with zero orphan; business receipts/items/stock/attendance simultaneously increased. The inspected Vercel package has no database action and targeted migration only adds nullable columns. Without actor/identity evidence, restoration would fabricate production state. |
| 2026-07-18 | Show global loading only for user-associated network work that remains pending beyond a short delay. | Direct owner complaint plus source evidence proves the 700 ms click pulse runs even without async work. Keep explicit authentication/lazy-route loading, but local clicks/selects/file choices must not flash or block the app. |
| 2026-07-18 | Preserve current account rows and repair visibility/future Auth-profile partial failure rather than deleting claimed conflicts. | Production read-only audit has 37/37 synchronized rows and no true orphan. Source nevertheless hides `active=false` profiles from the only account list and can create an orphan if Auth deletion fails after profile deletion. Show inactive login profiles only in account administration, keep them out of operational reporting, reorder deletion, recover only exact-email Auth users proven profile-less, and return actionable existing-account status otherwise. Do not mutate a production account without its exact username and owner intent. |
| 2026-07-18 | Preserve cake history/tombstones and repair catalog selection, not historical movements. | Production proves `cake-ready` history exists, its system catalog row was deliberately tombstoned, and the owner created active custom `TP-BANH`. The batch UI incorrectly filters mapped outputs to kg and Control Center misclassifies price-zero non-kg finished stock as menu. Resolve active configured outputs and compatible warehouse-finished fallback; do not rewrite past batches. |
| 2026-07-18 | Make attendance writes idempotent without weakening evidence rules. | Keep required native photo, GPS, concrete address and existing registration ownership. Add bounded retry/read-back proof so lost responses and zero-row updates cannot become false success/failure. Do not auto-edit the nine stale open production records. |
| 2026-07-17 | Add nullable requested delivery date/period to supply requests and reuse the existing status enum as kitchen confirmation/sent states. | Direct owner request adds desired receiving date plus morning/noon/afternoon, order date visibility, kitchen visibility and classified history. Keep new columns nullable so historical orders are not assigned invented dates. Preserve `pending → acknowledged → fulfilled → cancelled`; relabel them as waiting for kitchen confirmation, kitchen confirmed, kitchen sent, and cancelled instead of adding a redundant enum/schema transition. |
| 2026-07-17 | Recover obsolete lazy-route chunks with one automatic reload, then an explicit error screen. | Owner reports post-login white screen fixed by manual reload; production proves an old Kitchen chunk is 404 while the current one is 200. Retry only recognized dynamic-import/chunk-load failures and guard it with session storage to prevent loops. If the new bundle still cannot load, render a retry button instead of a blank root. Do not change auth, role or route permission rules. |
| 2026-07-17 | Treat `Chốt báo cáo` as a per-shift Zalo-delivery intent, while leaving the external connection unwired in this change. | Direct owner rule: Ca 1 records only a Ca 1 Zalo intent, completes its own handover and must not close/alter Ca 2 or the operation day; Ca 2 records Ca 2 plus Total-day intents and closes that business day. Preserve the required inventory/photo handover gate, ownership checks and Ca 2 no-open-shift guard. Store a durable `pending_connection` intent in the report snapshot; do not call n8n/Zalo from the finalize action so the owner's collaborator has an explicit integration seam. |
| 2026-07-17 | Keep decimal hours for payroll arithmetic, but display employee time as hours and minutes. | The owner's 13:55–22:20 example is exactly 505 minutes = 8 hours 25 minutes = 8.42 decimal hours. Changing the stored/calculated value would break valid wage arithmetic; change only human-facing duration labels and explicitly identify the decimal value where it is multiplied by hourly pay. |
| 2026-07-17 | Delete an erroneous attendance record only through an Admin-only audited RPC. | The owner directly requests a delete button for a mistakenly created employee/day attendance record. Require a reason and explicit employee/date confirmation; delete only the selected `attendance_records.id`, retain the shift registration, and save the record's complete before-state in the shared audit log. |
| 2026-07-17 | Do not award KPI merely for appearing in a revenue Top list. | Current rules award daily/weekly threshold performance; the Top list independently sorts revenue. The owner's question exposes a presentation ambiguity, not permission to invent a Top-N bonus. Show the computed reward/condition beside ranking and leave monetary thresholds unchanged unless the owner defines a new rule. |
| 2026-07-17 | Use one visible decimal symbol and no thousands grouping for inventory workbook numeric cells. | Owner reports mixed dot/comma values are ambiguous. Keep cells numeric and use `0.####` for quantities plus `0` for POS revenue; the viewer may render its configured decimal symbol, but no second grouping symbol is added. Values, formulas and units are unchanged. |
| 2026-07-17 | Make “Xuất bán và tồn bàn giao” use the inventory report's authoritative `from`/`to` period. | Owner directly requests date/month selection and identifies the one-day export limitation. Reuse the existing inclusive period across UI, sale-out, shift reconciliation and Excel instead of inventing another period state. Out/POS formulas and shift finality remain unchanged. |
| 2026-07-17 | Preserve Kitchen alert rules and make only the first-interaction audio prime reliably silent. | Owner authorized the fix/deploy after BUG-059 diagnosis. Set `muted` before the unlock `play`, pause/reset, then restore state. New-order alerts and the pending-only 60-second reminder remain unchanged; no order state, timing, role or data rule changed. |
| 2026-07-17 | Do not repair or force the linked Supabase migration history during this deployment. | Read-only runtime audits prove all 30 required tables, all 17 required functions and the latest migration objects are installed, while local eight-digit/repeated versions do not align with remote fourteen-digit history and `db push --dry-run` refuses. Reconciliation requires its own reviewed backup/mapping plan; this frontend-only release needs zero migrations. |
| 2026-07-17 | Randomly select one loading mascot from reference images 1/2/4 per loading session; reserve image 3 for attendance reminders. | Direct owner clarification. The loader preloads all three assets and chooses one only when a new loading session becomes visible; image 3 replaces only the check-in/check-out reminder art. Existing operations, attendance rules, disabled states and background refresh behavior remain unchanged. |
| 2026-07-11 | Reconstruct a minimal testing workflow and tracking set, then continue testing. | `docs/testing/` was absent from the worktree, `git ls-tree HEAD`, `git log --all -- docs/testing`, and repository-wide filename search. The reconstruction uses `AGENTS.md` plus the user's explicit requirements and is marked provisional. |
| 2026-07-11 | Preserve all pre-existing dirty-worktree files as user-owned. | Initial `git status --short` showed many modified/untracked source, migrations, scripts, artifacts, and data files. Only new `docs/testing/` files and deliberate test/fix files will be changed. |
| 2026-07-11 | Test authorization before other business modules. | Authorization spans UI navigation, branch scoping, RLS, and RPCs; a defect has Critical impact and can invalidate downstream test results. |
| 2026-07-11 | Do not treat the transient disk-space tool error as a product bug. | One source-read command returned OS error 112; smaller subsequent commands succeeded. It is an environment/tooling issue pending recurrence. |
| 2026-07-11 | Enforce business-logic preservation for every test and fix. | Direct user requirement added to `AGENTS.md`. Test/business conflicts become `Needs Business Confirmation`; fixes requiring business changes become `Blocked — Requires Business Decision`; no such logic is changed without direct approval. |
| 2026-07-11 | Do not use standalone browser automation while the selected Browser skill has no available in-app browser. | Browser runtime discovery returned `[]`; skill instructions require reporting this plainly rather than switching control backends. Continue non-browser authorization cases and leave UI cases Blocked. |
| 2026-07-11 | Leave manager branch scope unchanged and mark MOD02-TC-DB-01 `Needs Business Confirmation`. | Latest migrations explicitly grant managers all branches, but frontend session hydration/dashboard filtering uses profile/assignment branch IDs. Selecting either rule would reinterpret permissions, which `AGENTS.md` forbids without direct approval. |
| 2026-07-11 | Do not fix BUG-002 without direct approval. | Static contract test proves inactive profiles are not rejected by shared DB authorization helpers, contrary to the branch-deactivation old-session rule. Any correction changes schema/permissions, explicitly prohibited by `AGENTS.md`; status is `Blocked — Requires Business Decision`. |
| 2026-07-11 | Do not fix BUG-003 without direct approval. | LAN actor fallback trusts spoofable identity/role headers and protected routes do not consistently require authenticated sessions. Enforcing Bearer sessions changes API authentication/permissions, explicitly prohibited without approval; status is `Blocked — Requires Business Decision`. |
| 2026-07-13 | Keep all changes local and do not deploy. | The owner explicitly requested testing before deployment. Local static tests and production build are allowed; no Vercel/Supabase deployment or production migration is authorized. |
| 2026-07-13 | Do not invent a Zalo GMF image endpoint or expose OA secrets in frontend code. | Official docs reviewed expose GMF text messaging and OA image upload but no public contract for sending an image to a GMF group. Image automation remains blocked until Zalo confirms the endpoint/permission; secrets must be server-side. |
| 2026-07-13 | Route existing report-poster images through the owner-configured n8n webhook instead of inventing a Zalo image API. | The owner explicitly supplied the n8n workflow and fixed schedules. The browser will capture the existing `ReportPoster`; an authenticated same-origin server proxy will add the private webhook token and server-controlled schedule; n8n/Drive/Sheet handle delayed delivery. No database schema or production deployment is required. |
| 2026-07-14 | Revert attendance capture to the phone's native camera and request location automatically during check-in/out. | The owner explicitly rejected the live filtered-camera UI and manual Safari permission panel after the phone camera rendered black. This supersedes the 2026-07-13 live-camera/explicit-permission UI decision. GPS coordinates remain stored for audit accuracy, but the user-visible/photo location must be a concrete reverse-geocoded address. |
| 2026-07-14 | Keep report-finalization rules unchanged while making blocked/retry states actionable. | The owner reported that Finalize report cannot be pressed. Source evidence shows the UI hard-disables the button before its existing validation can explain the blocker, and Ca 2 can be stranded after its shift snapshot is saved before daily finalization fails. The fix may expose the existing validation reason and resume an interrupted Ca 2 finalization, but must not loosen closed-shift, ownership, or no-open-shift rules. |
| 2026-07-14 | Enable the already configured production n8n webhook locally without sending a synthetic test payload. | The owner reported no Zalo delivery after finalization. `.env.local` had a production HTTPS webhook URL and token but `N8N_REPORT_ENABLED=false`; this deterministically returns `mode: disabled`. The flag is set to true, but verification must use the existing “Gửi lại ảnh Zalo” action after the local server restarts so no unapproved synthetic report reaches the real group. |
| 2026-07-14 | Restore a single browser geolocation request for Safari. | The owner again reports GPS denied despite device location being enabled. Source still started `getCurrentPosition` and `watchPosition` concurrently, unlike the requested old automatic flow. Use one high-accuracy request, retain the 150m accuracy rule, and distinguish HTTP secure-context blocking from a real per-site permission denial. |
| 2026-07-14 | Keep the established n8n Header Auth contract and correct the Webhook credential instead of changing payload/API behavior. | The real owner retry reaches n8n but returns 403. Both server implementations and the existing regression contract send `x-gustino-token`; environment metadata confirms a production HTTPS URL and token are loaded. n8n officially returns 403 for failed authentication or a disallowed IP whitelist. Changing report payload/schedule is unrelated and prohibited. |
| 2026-07-14 | Add a manual send-now override while preserving automatic schedules. | Owner explicitly requests immediate sending after the queued-state button disappeared. The override may bypass the previous queued short-circuit only after an explicit duplicate warning, reuse the same job key for upsert, and set current UTC+7 `send_at`; default finalization remains 15:15/22:00/22:15. n8n must respond after downstream nodes, not immediately, to prevent false success. |
| 2026-07-14 | Do not mutate n8n until its Public API can produce a verified backup. | The temporary key is locally present and valid-looking, but every protected Public API path returns the same pre-auth HTTP 400 `Invalid URL`; Browser runtime also has no controllable session. Updating through undocumented REST endpoints or extracting Simple Browser cookies would bypass the supported authentication path. Repair the hosted Public API (or use a supported in-app Browser), then backup before the minimal mapping patch. |
| 2026-07-14 | Replace the report action cluster with a dropdown and essential actions only. | The owner directly requested fewer buttons, an actual Ca 1/Ca 2/Total day dropdown and repaired phone CSS. Remove the redundant manual refresh from this realtime page, merge retry/send-now into one explicit `Gửi Zalo` action, and render completed finalization as status rather than a disabled button. This changes presentation only: report formulas, leader-owned finalization and n8n proof requirements remain intact. |
| 2026-07-14 | Keep strict n8n completion proof and expose compact, differentiated diagnostics. | The owner has already experienced false success without Sheet/Zalo output. A production Editor panel without input data is expected and does not justify accepting HTTP 200 alone. Preserve exact `job_key` plus READY/row number; classify the existing response errors and place the bounded technical message in a collapsed details element so phone UI stays compact. |
| 2026-07-14 | Repair sales/report consistency only through complete authorized reads and pre-finalization freshness. | The deployed and local code cap daily receipts at 120 and finalize from already-rendered state. Paginate the same RLS-scoped query and rebuild the snapshot from a fresh branch/date ledger read. Do not change product prices, revenue/KPI formulas, seller identity rules, finalization gates, roles, schema or API contracts. |
| 2026-07-14 | Do not deploy the full dirty worktree as an urgent two-file hotfix without direct approval. | Official Vercel inspection proves the live deployment source can be isolated, but the environment usage limit blocked extracting the exact production files locally. A normal root deployment would include many unrelated attendance/report/n8n/API changes and violate the prior local-first deployment constraint. Keep production unchanged until the owner explicitly chooses the broader release or the isolated hotfix path is unblocked. |
| 2026-07-15 | Superseded interpretation — add three blank meal columns. | An ambiguous sentence was incorrectly read as a request for blank editable meal columns. The owner explicitly corrected this later the same day; this entry is historical only and is superseded by the removal decision below. |
| 2026-07-15 | Supersede the prior competition-hours and client-only auto-finalization interpretation. | The owner explicitly states competition must not display registered hours and day closing must be automatic rather than dependent on opening the Report page. Remove the visible hours/load from competition and replace the `sessionStorage` handoff with a server-scheduled close path while preserving the existing finalization settlement rules. Restore the owner-requested legacy attendance photo stamp presentation. |
| 2026-07-15 | Supersede the prior blank-meal-column interpretation. | The owner explicitly states those columns were not requested. Remove Bữa sáng/Bữa trưa/Bữa tối from every attendance XLSX summary/detail surface; do not calculate, store or expose meal data. Preserve the selected date-range export, attendance formulas and evidence links. |
| 2026-07-15 | Show current system stock separately on the count voucher; do not prefill physical counts. | The owner wants the voucher image to report what is currently in the warehouse. Pre-populating Kho đông/Kho phòng would silently turn system stock into an asserted physical count when saved. Add a dedicated read-only current-stock column and image share/download action instead; counting inputs and stock formulas remain unchanged. |
| 2026-07-15 | Auto-close only after the business date has ended. | Direct owner clarification supersedes the provisional 22:20 same-day schedule. Configure 00:00 UTC+7 (`0 17 * * *` in Vercel UTC) and calculate the previous UTC+7 calendar date inside the endpoint. Keep catch-up for older stale open days and never include the newly started date. |
| 2026-07-15 | Treat current source permissions as testable behavior, but not as confirmed business truth where repository documents conflict. | `App.tsx`/`AppShell.tsx` currently make manager Orders and Report Archive admin-only, while `BA.md` and `CODEMAP.md` §28 say managers handle orders and retain both surfaces. QA mirrors were corrected to current source only; application permission changes remain `Needs Business Confirmation`. |
| 2026-07-15 | Do not implement incomplete BA workflows that require new state/schema/permission rules during bug testing. | Dynamic RBAC enforcement, cancellation reasons/audit, large inventory-variance approval and payroll period lock are documented requirements but not complete current contracts. Implementing any requires choices about schema, transitions, thresholds or authorization, prohibited without direct approval. |
| 2026-07-15 | Do not change the Supabase POS delete authorization RPC without direct approval. | The same-day rule uses database `current_date` rather than UTC+7 business date. Client/LAN date-source defects can be fixed without changing rules; the server correction requires a migration and authorization-function change, so BUG-038 remains blocked. |
| 2026-07-15 | Attribute leader competition to total revenue inside leader-owned shift sessions. | The owner explicitly clarifies that a leader's ranking is the total revenue of Ca 1/Ca 2 they led, not the leader's personal receipts. Use existing `bag_shift_sessions` ownership/timestamps and POS receipts; reuse existing leader daily KPI targets. This direct instruction authorizes the ranking correction but does not authorize new payroll thresholds. |
| 2026-07-15 | Remove manager branch-invoice drilldown and deploy-facing technical notes. | The owner explicitly asks to remove branch invoice viewing for manager and all developer-note copy. Keep aggregate dashboard metrics/hourly counts and friendly action feedback; raw n8n detail remains in logs/server responses rather than the operational toolbar. This supersedes the 2026-07-14 decision to expose a collapsed technical detail element. |
| 2026-07-15 | Fix KPI reward aggregation without changing reward thresholds. | Reverse audit proves direct POS receipts were counted as separate days/wins. Aggregate allocation and direct-POS revenue by date × branch × employee before applying the existing daily/weekly functions once. Leave the currently unused monthly bonus at zero until the owner confirms a monthly-pay business rule. |
| 2026-07-15 | Treat `disabled` as a static unavailable state, not proof of loading. | Source has many buttons disabled because prerequisites/data/permissions are absent, but global CSS gives every disabled button a spinner and progress cursor. Remove only that visual conflation; keep all disabled expressions, handlers and actual async “Đang…” labels unchanged. |
| 2026-07-15 | Do not recommend production deployment until midnight auto-close secrets exist. | The final UI/build passes, but authoritative `vercel env ls` exposes only public Supabase variables and local names-only inspection also lacks `SUPABASE_SERVICE_ROLE_KEY`/`CRON_SECRET`. Deploying now would knowingly leave the owner-required automatic close non-operational. |
| 2026-07-15 | Deploy only the inspected Vercel prebuilt output after direct owner authorization. | The owner explicitly requested deployment without affecting active data. Prebuilt inspection contained only static assets and five expected API functions, with no migration/seed/purge/data artifacts; missing cron secrets stop auto-close before database access. Deploy the prebuilt output, then perform read-only alias/index/API/CSS verification only. |
| 2026-07-15 | Require a post-build Supabase bundle guard before every prebuilt production deploy. | A stale generated `.vercel/.env.production.local` silently set both public Vite Supabase values empty, producing a valid build that made intact cloud data appear lost. The standard `npm run build` now runs `test-production-supabase-bundle.mjs`; local prebuild must also explicitly load the two public Vite values and inspect `.vercel/output`. Never restore/seed data in response to an empty UI without database/RLS evidence. |
| 2026-07-15 | Active-user presence and attendance-adjustment requests are cloud-only. | The owner directly approved the existing additive production migration and prohibited localStorage because device-local success is not synchronized. Both modules now fail visibly when cloud persistence is unavailable; no local/missing-table success fallback remains. This decision is scoped to these two flows and does not reinterpret unrelated local/demo behavior. |
| 2026-07-16 | Keep all branch summaries visible while inspecting one warehouse. | The owner reports that opening inventory detail makes branches disappear. Current inline `details` inserts a potentially long SKU block before the next branch. Use a stable branch-card grid and render the selected detail once below the complete grid, matching common warehouse dashboards. This is presentation-only; retain every current filter, stock/low-stock/waste/movement calculation, permission and write path. |
| 2026-07-16 | Correct attendance only at the authoritative cloud record and audit every Admin edit. | The owner directly authorizes Admin-only attendance correction and requires payroll synchronization. `AdminPage` and payroll already derive from the same `attendance_records`; update only check-in/check-out timestamps through a guarded RPC with mandatory reason and audit entry, then realtime-refresh the existing views. Do not create a browser-local correction copy or change pay/KPI formulas. |
| 2026-07-16 | Use export-only fixed-size posters for operational images. | Interactive inventory-count and order forms are not reliable image canvases and currently produce variable aspect/empty space. Keep entry forms unchanged in behavior, but render fixed 9:16 inventory posters and a readable 4:5 order poster, export as PNG at 2× scale, and show branch/reporter prominently. |
| 2026-07-16 | Supersede fixed 9:16 for the inventory-count image with content-driven height. | The owner directly rejects the long empty result and supplies the compact paper form as reference. Preserve the export-only sharp PNG approach, but calculate height from the rendered row count/content and use the white paper/green accent/dark table hierarchy. This does not change the separate order export or inventory business data. |
| 2026-07-16 | Keep attendance correction navigation separate from the report/export range. | The owner wants correction by one day or one employee because a multi-month list is too long. Keep the top date range as the authoritative report/Excel/payroll scope; inside the correction list, default to an exact day, offer a one-employee mode and paginate 20 rows. This is presentation/navigation only and must not change attendance records, validation, audit or payroll formulas. |
| 2026-07-16 | Never present mixed inventory units as one total. | The owner cannot distinguish kg, grams and item counts in the manager cards. Group displayed quantities by product unit, convert sub-kilogram display to grams and keep raw stored quantities/formulas unchanged. The exact-day sales-issue table must use only existing `sale_out`; processing, packing and waste remain separate movements. |
| 2026-07-16 | Replace separate daily/leader competition cards with one classified table. | The owner's direct requirement supersedes the prior static expectation for a fixed Top-3-today card. Classify one table as employee/day, employee/month or shift leader/month and allow an exact day inside the active range. Preserve aggregation functions, revenue-positive eligibility and KPI formulas. |
| 2026-07-16 | Do not silently convert every POS receipt into a warehouse `sale_out`. | Production proves current receipts have no allocation link, while the warehouse voucher and shift-close allocation path define `sale_out`. Automatically equating goods sold with goods issued from warehouse would change stock timing/semantics and could double deduct when a manual issue voucher also exists. Keep diagnosis read-only and request the owner's choice between manual warehouse issue, allocation-based close, or an explicitly designed POS auto-deduction rule. |
| 2026-07-16 | Use the existing handover balance for remaining end-of-shift goods; do not add a return flow. | Direct owner clarification: leftover goods are already counted during handover and inherited by the following shift/day. A completed-shift out/consumption reconciliation is opening + additions − closing handover − waste, compared with POS sales. This supersedes the suggestion to add a separate leftover-return function. |
- 2026-07-18: Owner explicitly rejected AI-invented dashboard/launcher/card/mobile-settings layouts and directed an Odoo Enterprise 18-like desktop ERP experience with CukCuk/KiotViet data density. The UI contract therefore fixes the Admin shell at sidebar + header + current workspace, prohibits sidebar icon-only collapse and launcher navigation, standardizes list modules on toolbar/search/filter/table/pagination, and limits branding changes to the supplied green palette. Business calculations, permissions, writes, API contracts and database schema remain out of scope.
| 2026-07-18 | Deploy the screenshot-corrected Admin release only after the attendance and prebuilt gates pass. | The owner explicitly requires immediate deployment and no check-in/check-out regression. Run the non-destructive attendance/idempotency/camera/GPS/schedule/cloud/realtime suite, keep the request-aware Capy overlay gated to fetches following user actions, deploy only the inspected prebuilt output, and perform read-only live checks. Do not run migrations, create synthetic attendance, invoke cron/webhooks or mutate production business data. |
| 2026-07-18 | Scope the new ERP design to Admin and preserve the existing Manager product experience. | Direct owner clarification supersedes the shared Admin/Manager shell introduced by the recent UI refactor. Admin continues to use the new durable `#/admin/...` workspace; Manager returns to the existing dashboard and focused Revenue/Business/Inventory routes. This is a role-specific presentation/navigation correction only and must not broaden data permissions or change formulas, schema or writes. |
| 2026-07-19 | Resolve displayed shift-leader identity from existing session-overlapping attendance before stored session text. | The owner reports Ca 2 duplicates Ca 1 at Lotte Vũng Tàu. Report generation already loads approved registrations and authoritative attendance. Use the checked-in leader nearest each session start independently, with the stored session leader as a safe fallback. This is a presentation correction only: do not rewrite historical sessions/attendance, alter shift ownership, or change report calculations/finalization. |

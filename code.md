# GUSTINO — Handoff công việc theo yêu cầu 2026-07-13

> Không deploy. Chỉ build/test local để chủ dự án kiểm tra trước.

## Đang làm

- [x] Popup chấm công code/static/type test: đọc ca hôm nay và nhắc đúng check-in/check-out, không nhầm với popup Chủ nhật. Còn visual test.
- [x] GPS iPhone code/static/type test: nút xin/kiểm tra quyền, phân biệt Safari chặn HTTP với người dùng tắt Vị trí chính xác. Còn HTTPS iPhone test.
- [x] Camera chấm công đã theo yêu cầu sửa lại ngày 2026-07-14: bỏ live preview/filter gây màn hình đen, dùng camera chụp gốc của điện thoại và giữ decode fallback. Còn test trực tiếp iPhone.
- [x] Supabase session code/static/type test: bật rõ `persistSession`, auto refresh token liên tục. Còn test thoát/mở Safari; LAN token sau khi restart server vẫn cần kiểm tra riêng.
- [x] Trang Kinh doanh code/static/type test: giữ top 10 tháng, thêm top 3 ngày và thi đua ca trưởng tháng; bỏ chart/bảng trùng. Còn visual/live-data test.
- [x] Đã sửa `7/0 sản phẩm`; test pass. Công thức KPI gốc giữ nguyên và khớp matrix đã lưu trong `CODEMAP.md`.
- [x] Chốt báo cáo theo ca code/static/type test: Ca 1 gửi một báo cáo; Ca 2 gửi báo cáo Ca 2 + Tổng ngày bằng một nút theo đúng ca trưởng/điều kiện. Còn UI/integration test.
- [x] Kết nối Zalo GMF phía server cho tin nhắn chữ, cả Supabase API và bản LAN local; giữ secret ngoài frontend. Cần `ZALO_OA_ACCESS_TOKEN` + `ZALO_GMF_GROUP_ID` để test thật.
- [x] Nối ảnh infographic hiện có sang n8n: Ca 1 xếp lịch 15:15; Ca 2 lúc 22:00; Tổng ngày lúc 22:15. Web tái sử dụng đúng `ReportPoster`, token webhook chỉ ở server, trạng thái từng ảnh có idempotency và nút retry. Static/type/regression/build đã pass.

- [x] MOD-06 code + static/type test: ảnh iPhone có decode fallback/EXIF orientation, dùng camera chụp gốc của điện thoại (đã bỏ live camera/filter), bỏ giới hạn giờ trong ngày, tự lấy GPS khi chấm công, giờ ưu tiên máy chủ, ảnh ghi chi nhánh/giờ/địa chỉ cụ thể và độ chính xác; tọa độ chỉ lưu ngầm để đối soát. Còn test trực tiếp iPhone 11 trên HTTPS.
- [x] MOD-13 sửa nút Chốt báo cáo: hiển thị lý do chưa đủ điều kiện, cho Ca 2 tiếp tục nếu lần trước lưu dở, và gộp snapshot Ca 2 vào luồng chốt Tổng ngày. Đã qua static/type/regression; còn test thật bằng tài khoản ca trưởng.
- [x] Đã bật `N8N_REPORT_ENABLED=true` trong `.env.local`, xác nhận URL production HTTPS + token có cấu hình và khởi động lại local dev/API. Báo cáo cũ cần bấm “Gửi lại ảnh Zalo”; chưa tự gửi payload test để tránh ảnh giả vào group thật.
- [ ] Gửi ảnh thật từ Codex chưa thực hiện được: kể cả sau khi chủ dự án xác nhận lại rủi ro, môi trường vẫn từ chối xuất ảnh + metadata kinh doanh tới webhook ngoài chưa xác minh. Không có request/job nào được tạo và không được thử đường vòng. Chủ dự án cần bấm “Gửi lại ảnh Zalo” trực tiếp trong web đang chạy.
- [x] Sửa local Supabase → n8n: API local nay xác thực token Supabase, kiểm tra ca/snapshot trên Supabase và lưu trạng thái queue về snapshot cloud. Thêm timeout 25 giây ở web, 20 giây ở webhook để không quay vô hạn. Đã restart local; còn bấm retry thật và kiểm tra n8n Execution.
- [x] Sửa lỗi web chỉ báo chung `Không thể đưa ảnh...` khi API local tắt: hiện giữ lỗi JSON thật, còn response không phải JSON sẽ ghi rõ mã HTTP và cách chạy lại API. Local 5173/5177 đã chạy, health 200 và proxy đã kiểm tra; còn chủ dự án reload rồi bấm retry thật.
- [ ] Retry thật đã tới n8n nhưng bị HTTP 403. Cần sửa Webhook node: `Authentication = Header Auth`, credential Name=`x-gustino-token`, Value phải trùng chính xác `N8N_REPORT_WEBHOOK_TOKEN`; tạm bỏ IP Whitelist, Save/Publish và giữ workflow Active. Local đã restart và health 200; chỉ cần restart lại nếu đổi `.env.local`.
- [ ] Ảnh n8n mới xác nhận: Options không có IP Whitelist; node Convert to File đã đúng. Dropdown credential chỉ là nhãn hiển thị — phải bấm bút chì kiểm tra Header `Name`/`Value` bên trong. Dùng Production URL, đặt `Respond=When Last Node Finishes`, Save/Publish Active rồi retry để web chỉ báo thành công sau khi Sheet chạy xong.
- [ ] Chủ dự án đã cho phép Codex tự cấu hình n8n, nhưng runtime không có tab in-app Browser nào (`[]`) và không thể điều khiển Chrome ngoài đang đăng nhập. Cần mở n8n trong Browser bên trong Codex rồi nhắn `đã mở`; không đổi backend/lách đăng nhập.
- [ ] Đã thử kết nối lại sau khi chủ dự án báo mở xong nhưng danh sách tab vẫn `[]`; chưa có thay đổi nào trên n8n. Cần xác nhận đúng panel Browser nội bộ của Codex, không phải tab Chrome thường.
- [x] Tìm được nguyên nhân 403 sau ảnh cấu hình đúng: `.env.local` sửa lúc 10:56 nhưng API còn chạy từ 10:42, nên server giữ token cũ. Cần restart local để nạp token mới rồi chủ dự án bấm retry một lần.
- [x] Đã đổi `.env.local` từ `/webhook-test/...` về Production `/webhook/...`, config test pass và restart lại. Health 200, server mới đã nạp env (`ENV_NEWER_THAN_SERVER=False`). Còn chủ dự án bấm `Gửi lại ảnh Zalo` một lần và xem n8n Executions.
- [x] BUG-017: API local/cloud nay giữ message/error JSON của n8n tối đa 240 ký tự, tự ẩn token và chỉ rõ xem Executions; test/type/syntax pass, local đã restart và health 200. Còn retry thật để lấy đúng node/lỗi n8n; không đổi payload/lịch/nghiệp vụ.
- [x] Retry ảnh thật Ca 1 đã thành công: web báo `Đã đưa 1 ảnh vào lịch gửi tự động n8n.`; đường web → API local/Supabase → production webhook đã qua, lỗi 403/500 hết. Còn xác nhận n8n Execution xanh, Sheet `READY` → `SENT` và ảnh thực tế xuất hiện trong group Zalo; chưa test gói 2 ảnh của Ca 2.
- [ ] Đính chính: Sheet không có dòng nên banner trên chỉ là Webhook nhận request, chưa phải queue hoàn chỉnh. BUG-018 đã sửa trong code và local đã restart: nút `Gửi ngay Zalo` luôn còn, có cảnh báo gửi trùng, giữ cùng job key, đặt giờ hiện tại UTC+7 và bypass idempotency chỉ cho thao tác này; lịch tự động cũ giữ nguyên. Còn chờ chủ dự án đặt n8n `When Last Node Finishes` và kiểm tra Sheet/Zalo thật.
- [ ] Blocker n8n mới có ảnh chụp xác nhận: node cập nhật Sheet tham chiếu node cũ đã không còn. Ảnh mới cho thấy `Column to match=job_key`, nên ô `job_key (using to match)` phải là `{{ $('Limit1').first().json.job_key }}`, không phải `.json.row_number`. Chạy `Limit1` để kiểm tra preview, Save/Publish rồi retry.
- [ ] `Limit1`/Update row bị xám vì đây là nhánh gửi theo lịch, trong khi Sheet chưa có dòng `READY`. Không sửa tiếp node cuối trước; cần chạy nhánh Webhook → Convert → Upload → Append/update Sheet để tạo dòng, rồi mới test nhánh quét phút và cập nhật `SENT`.
- [ ] Ảnh node `Append or update row in sheet` hiện không có lỗi biểu thức đỏ: field lấy từ `Webhook.body`, `drive_file_id` lấy từ `Upload file.id`. INPUT xám là schema dự đoán vì chưa có execution. Save/Publish, bấm `Gửi ngay Zalo` trên web để gọi production webhook, rồi xem execution mới và dòng Sheet thật; không Execute riêng node cuối.
- [ ] Sau báo cáo “chưa thấy động tĩnh”, local vẫn OK: config production pass, web/API 200, cổng 5173/5177 lắng nghe. Cần ảnh n8n `Executions` ngay sau một lần bấm `Gửi ngay Zalo`: không có execution mới = workflow chưa Active/Production URL chưa nhận; có execution = mở node đỏ đầu tiên.
- [ ] Ảnh Executions mới chỉ có các lượt `quét 1 phút`; nhánh Webhook dưới màu xám, nghĩa là web chưa đi vào workflow. Local đã nạp đúng `haehfy.ezn8n.com/webhook/gustino-report-ready` sau lần sửa env. Cần hard refresh trang local `/#report`, bấm và xác nhận `Gửi ngay Zalo`, rồi chụp đúng banner cạnh nút để biết request bị chặn trước webhook hay không.
- [ ] Ảnh 13:30:02 lần nữa xác nhận chỉ cron/Get rows chạy; Sheet trống nên Code/Limit/Drive/Zalo xám, Webhook dưới cũng xám. Không chờ thêm n8n. Cần ảnh popup xác nhận và banner trên trang GUSTINO sau đúng một lần bấm `Gửi ngay Zalo`.
- [x] Banner thật đã xuất hiện: `Đã yêu cầu n8n gửi ngay 1 ảnh Zalo.` — web/client/auth/API/production webhook của Ca 1 đã qua. Còn mở đúng Webhook execution vừa tạo và xem node `Append or update row in sheet`: Document, Sheet và OUTPUT; node xanh có row number nhưng Sheet đang xem trống nghĩa là ghi nhầm file/tab.
- [ ] Ảnh “không có output” hiện tại là Editor/Preview (có dòng `Usually outputs... Execute the node` và schema capabilities), không phải execution. Phải vào Executions → chọn lượt Webhook không phải cron → bấm node trong execution mới đọc được INPUT/OUTPUT thật.
- [ ] BUG-018 mở lại: backend mới chỉ chặn chuỗi “workflow started”, vẫn có thể nhận 2xx rỗng/nhầm node và báo thành công. Test mới đang fail 2 assertion cloud/LAN; sửa để chỉ `queued=true` khi response cuối có đúng `job_key` và `row_number` của Sheet.
- [x] BUG-018 completion proof đã sửa: cloud/LAN chỉ `queued=true` nếu JSON cuối có đúng `job_key` + `row_number`; 2xx rỗng, không phải JSON, early ACK hoặc sai job đều báo lỗi. Queue/type/syntax và 4 regression liên quan pass; còn restart local và retry thật.
- [x] Local đã restart sau completion proof: PID 26932, web/API 200, 5173/5177 listen, config pass. Lần bấm tiếp theo không thể báo thành công giả; banner sẽ chỉ thành công khi Sheet trả đúng dòng hoặc nêu contract response còn thiếu.
- [ ] Retry thật đã lộ nguyên nhân n8n: Sheet tạo dòng nhưng ghi nguyên văn `{{ ... }}`; các field động đang Fixed mode, chỉ `report_label` có fx nên chạy đúng. Chuyển job/branch/date/type/send/drive/file sang Expression fx (`.first()`), giữ `READY`/`0` Fixed, xóa dòng test sai, Publish và gửi lại.
- [ ] Ảnh selector đã xác nhận: `Fixed` đang sáng; sau khi sửa, mỗi field động phải có dấu `fx` giống `report_label`. Hover từng field nếu chưa thấy selector rồi bấm `Expression`.
- [ ] Sau khi đổi Expression vẫn chưa hoàn tất: Google Sheets node có thể không trả `row_number`. Test mới fail 2 assertion; sửa guard để nhận đúng `job_key` + (`row_number` hoặc `status=READY`), vẫn chặn mọi 2xx rỗng/sai job.
- [x] Đã sửa tương thích output Sheet: nhận đúng `job_key` + (`row_number` hoặc `status=READY`), vẫn chặn 2xx rỗng/sai job/early. Queue/type/syntax và 4 regression pass; còn restart local và retry thật.
- [x] Local đã restart bản tương thích output dưới PID 11388; 5173/5177 listen, web/API 200, config pass. Hard refresh và retry một lần; không cần thêm node chỉ để tạo `row_number`.
- [ ] Chủ dự án yêu cầu Codex tự sửa n8n nhưng Browser điều khiển vẫn trả `[]`; tab hiện tại là Chrome ngoài Codex. Cần mở/đăng nhập n8n trong in-app Browser của Codex, sau đó Codex mới bấm Expression cho field động và Publish được; không được lấy cookie/token Chrome để lách.
- [ ] Ảnh mới xác nhận đây là VS Code Simple Browser, Browser runtime vẫn không điều khiển được. Chuyển sang API chính thức: chủ dự án tạo key tạm và chỉ thêm `N8N_API_KEY=...` vào `.env.local`; Codex sẽ GET/backup workflow `mjjUXcsws-DT13Y2wCoqK`, patch đúng node, verify/publish, không in key, xong thì thu hồi key.
- [ ] `N8N_API_KEY` đã được thêm đúng file ignored và còn hạn, nhưng n8n Public API hiện hỏng trước xác thực: workflows/users/tags đều trả HTTP 400 `Invalid URL`, kể cả request không có key; docs/OpenAPI/health vẫn 200. Chưa GET được workflow nên chưa có backup, chưa PUT/Publish. Cần sửa/upgrade cấu hình Public API hoặc reverse proxy trên máy chủ n8n; sau đó Codex mới backup và patch an toàn.
- [x] n8n đang dùng production URL `/webhook/...`: phải bật workflow **Active** và xem **Executions**; không dùng “Execute workflow/Listen for test event” vì nút đó chỉ chờ `/webhook-test/...` nên sẽ quay mãi.
- [ ] Safari iPhone chỉ cho GPS trên HTTPS. Link LAN `http://192.168.100.32:5173` dùng được camera nhưng không thể dùng định vị; cần chủ dự án cho phép HTTPS tunnel/preview hoặc deploy trước khi test GPS thật.
- [x] MOD-08 code + static/type test: ca trưởng check-in tự mở ca, Bàn giao tự phục hồi không cần nút Nhận ca, ngày mới tự đóng ca treo cũ và sequence bắt đầu Ca 1, ảnh đầu/cuối ca nằm ở bước 1/bước cuối trang Hôm nay, vẫn giữ kiểm tồn/Bàn giao. Còn integration QA.
- [x] MOD-10 code + static/type test: bỏ thanh mức độ, chuẩn hóa âm 0, tồn kg nhỏ hiện gram, phiếu nhập/xuất chọn kg hoặc gram và có cảnh báo cách nhập `5,123 kg`. Còn visual QA.
- [x] MOD-13 code + static/type test: lọc Tổng ngày/Ca 1/Ca 2, xuất tên file riêng theo ca, ranking chỉ có người bán thật, bỏ giờ, mobile đông người về 1 cột. Còn visual QA và kiểm tra dữ liệu Supabase thực.
- [x] MOD-17 code + static/type test: report và kho dùng Blob/File + Share Sheet trên iPhone, fallback tải file chuẩn, không còn báo đã lưu khi chưa xác nhận thao tác. Còn test trực tiếp iPhone.
- [x] Realtime code + static/type test: chấm công, báo cáo và trang Hôm nay có subscription/polling/focus refresh; riêng Hôm nay fallback 8 giây cho ca và POS. Còn test hai thiết bị thật.
- [x] Nhắc việc code + static/type test: popup đăng ký ca chỉ Chủ nhật; nhắc chấm công lấp lánh và capybara CSS chuyển động (không tải asset nặng). Còn visual QA.

## Chưa thể hoàn tất nếu thiếu thông tin ngoài repository

- [ ] Chạy end-to-end thật cho ảnh qua n8n: cần chủ dự án chốt thử một báo cáo trên local để xác nhận Webhook → Drive → Sheet → node Zalo → `SENT`. `N8N_REPORT_ENABLED=true`, vì vậy không tạo payload giả tự động để tránh gửi nhầm vào group. Chưa deploy.
- [ ] Nếu sau này bỏ n8n và gửi ảnh trực tiếp bằng Zalo OA/GMF: **Blocked — Requires Zalo API/Permission Confirmation** vì tài liệu công khai chưa có contract ảnh GMF. Luồng n8n hiện tại không phụ thuộc contract này.
- [ ] Xác minh trực tiếp lỗi riêng trên iPhone 11: cần chủ dự án test bản local/staging trên đúng máy sau khi build; automation desktop không mô phỏng đầy đủ camera, GPS, Safari Share Sheet và quyền iOS.
- [ ] Xác minh điện thoại mở bản local: Safari vào `http://192.168.100.32:5173/` khi cùng Wi-Fi; hướng dẫn và bằng chứng mạng ở `docs/LOCAL_PHONE_TEST.md`. Nếu mở trang được nhưng GPS bị chặn do HTTP thì cần chủ dự án cho phép tạo HTTPS test URL, vẫn không deploy production.
- [x] BUG-019: trang Báo cáo nay dùng một dropdown Ca 1/Ca 2/Tổng cả ngày, còn `Tải ảnh`, một nút `Gửi Zalo` và trạng thái chốt; bỏ đồng bộ thủ công cùng hai nút Zalo trùng nhau. CSS mobile cuối file chặn tràn ngang; regression/type/build pass. Còn kiểm tra trực tiếp trên điện thoại vì Browser runtime không có tab khả dụng.
- [ ] n8n vẫn chưa thể tự sửa qua API dù có `N8N_API_KEY`: khóa này không được luồng gửi báo cáo sử dụng, và máy n8n hiện trả pre-auth HTTP 400 `Invalid URL` cho Public API. Luồng thật dùng `N8N_REPORT_WEBHOOK_URL` + token và đã tới webhook; node cuối phải trả JSON đã đánh giá có đúng `job_key` cùng `status: READY` hoặc `row_number`. Không được để các chuỗi `{{ ... }}` ở Fixed mode.
- [ ] Retry thật sau giao diện tinh gọn vẫn báo n8n chưa xác nhận lưu. Public API execution query bằng key cấu hình tiếp tục trả HTTP 400 `Invalid URL`; không thể tự đọc/sửa workflow. Cần node cuối sau Google Sheet trả trực tiếp `{ job_key: <giá trị thật từ Webhook>, status: 'READY' }`, Webhook `When Last Node Finishes`, rồi Publish. Chụp OUTPUT JSON của node cuối trong đúng Webhook execution nếu vẫn lỗi.
- [x] BUG-020: tách thông báo n8n thành response rỗng/sai JSON/phản hồi sớm/sai job_key-status; thêm `Chi tiết lỗi n8n` thu gọn chứa message gốc. Regression/type/build pass, strict completion proof giữ nguyên. Còn bấm `Gửi Zalo` thật một lần và gửi lại nội dung chi tiết nếu vẫn lỗi.
- [ ] Áp dụng migration Supabase mới (nếu có): chỉ thực hiện sau khi chủ dự án duyệt và xác nhận môi trường không phải production.

## Ràng buộc

- Không deploy cho tới khi chủ dự án yêu cầu trực tiếp.
- Không đổi role/permission, công thức KPI/doanh thu/tồn kho hoặc dữ liệu lịch sử ngoài yêu cầu đã nêu.
- Mọi token/API key phải đặt bằng biến môi trường, không commit.
